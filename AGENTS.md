# Finance Tracker Agent 工作流

本仓库采用 **OpenSpec + 仓库自有工作流**。`AGENTS.md` 是变更流程、审查、验证和外部写授权的唯一规则；`openspec/project-context.md` 是跨变更的财务、持久化与安全工程原则；`openspec/specs/` 是当前行为的唯一事实源。`CLAUDE.md` 是本文件的符号链接，修改 Agent 约定时只编辑本文件。

所有门禁均为强约束。任务持续推进，直到完成、缺少关键决策、外部授权或必需环境条件。项目 Skill 只承载领域知识、OpenSpec CLI 或外部工具操作，不取代本文件的 artifact、审查、QA、验证或授权。

## 需求澄清门禁

- 每项变更在开始实施前，都必须显式调用项目内的 `grill-me` 技能，并确保其依赖的 `grilling` skill 已安装；持续进行需求澄清，直到目标、范围、非目标、具体内容、验收标准、边界条件和关键风险明确。`grill-me` 是用户入口，`grilling` 是实际访谈 skill；在 Codex 中使用 `$grill-me` / `$grilling` 调用，不要把它们当作 shell 命令。
- 该门禁适用于代码、测试、规格、文档、配置、技能、依赖和迁移等所有仓库变更；仅阅读、查询状态和运行只读验证不属于实施变更。
- 澄清结论必须写入对应的 OpenSpec `proposal.md`、`design.md`、`tasks.md` 或其他正式变更记录。若仍存在会改变范围、语义或验收标准的歧义，必须暂停实施并请求用户决策。
- `grill-me` 默认禁止隐式调用，代理必须显式调用该技能并由 `grilling` 执行逐轮澄清，不能以自行推测或简短确认替代需求澄清。

## 工具边界

- 环境必须提供 OpenSpec CLI `1.7.0+` 与 Node.js `20.19.0+`；初始化或升级后以 `openspec --version` 确认。OpenSpec CLI 在终端运行，`$openspec-*` 必须按 Codex Skill 调用，不能作为 shell 子命令。
- 项目不依赖 gstack 或其他仓库外 Skill。唯一例外是 Hallmark：A 类 UI 原型调用 `$hallmark` 技能，任何等级的 UI 审查调用 Hallmark 的 `audit` 技能动作（通常写作 `$hallmark audit`）。Hallmark 是由代理运行时提供的技能，不要求仓库内存在 `hallmark` CLI；`$hallmark` / `$hallmark audit` 都是技能调用，不得改写成 shell 命令或以 `command -v hallmark` 作为技能是否可用的判据。若当前运行时未提供该技能，必须记录技能不可用并完成可替代的人工审查，不得声称已执行 Hallmark。
- Web QA 使用仓库实际提供的 Vitest、Playwright、生产预览和适用浏览器工具；缺少某个浏览器 Skill 时，改用可用工具完成同等检查。
- PostgreSQL 双后端验证属于交付前门禁，不由测试代码自动发现或自动启动数据库。完成开发和 SQLite 回归后，执行者必须在本机准备专用测试库：优先使用本地 Docker PostgreSQL 或本机 `psql` 可连接的数据库，将连接配置到 `FT_TEST_POSTGRES_URL` 后补跑同一契约矩阵；数据库名必须以 `_test` 结尾。未配置时只能记录为未完成，不能把跳过项计入完整验证。
- 必须提交 `openspec/`、`.agents/skills/openspec-*`、`openspec/specs/` 与 `openspec/changes/`；运行缓存、凭据、浏览器状态与真实财务数据留在仓库外。

## Web 浏览器 QA 必须门禁

- 任何 Web UI、交互、路由、响应式布局或用户可见文案修改后，必须使用真实浏览器完成 QA；Vitest、TypeScript 检查、生产构建或静态截图不能替代真实浏览器 QA。
- 浏览器 QA 至少覆盖受影响主流程、正常/错误/空状态、关键点击与键盘焦点，以及相关桌面和移动宽度；涉及登录、权限、滚动或跨页面导航时必须实际走通对应流程。
- 优先使用生产构建预览和仓库实际浏览器工具；浏览器不可用时必须记录为未完成或阻断，不得宣称“已通过”。
- 在对应 OpenSpec `tasks.md` 记录浏览器、URL、视口尺寸、测试步骤、截图路径、控制台/网络错误和结果；代码或依赖变化后必须重新运行受影响的浏览器 QA。

## 中文术语与文案

修改文档、注释、docstring 或程序文案时，先使用 `$domain-glossary` 读取词表、主规格、active change 和代码上下文；新概念、语义变化或歧义先更新 `DOMAIN_GLOSSARY.md`。随后按 `$chinese-documentation` 复核本次适用文本。代码标识符、字段、参数、枚举、协议字面量和原文引用保持原样并使用反引号；`leg` 按业务语境写为**现金部分**、**证券部分**、**付出资产**、**换入资产**或具体流水。

## 变更分级

分级只调整八阶段的深度，不降低财务正确性、数据安全、公共契约或可回滚性。分类存疑时使用 A 类。

| 等级 | 适用条件 | OpenSpec 与最小验证 |
|------|----------|--------------------|
| A：完整变更 | 新领域能力、领域规则、跨模块重构、公共接口或依赖边界、财务计算、币种与精度、数据模型、持久化、迁移、兼容性、审计、安全敏感变更，或超过 3 个独立模块。 | 完整 change：`explore → propose → validate → apply → archive`；受影响测试、完整回归、类型检查、构建、`git diff --check`、适用双后端矩阵、Web QA、性能与安全检查和独立复核。 |
| B：轻量变更 | 既有架构内的局部能力，不改变领域规则、金额语义、持久化、权限、路由、公共契约或依赖。 | 小型 change 记录目标、非目标、验收、受影响文件、风险和证据；受影响测试、相称构建、`git diff --check` 和范围化 diff 复核。 |
| C：局部缺陷修复 | 根因明确、恢复既定行为，且少量回归测试可以证明。 | 最小 change；先复现并写失败回归测试，再作最小修复；新增回归测试、受影响测试、相称构建、`git diff --check` 和短范围 diff 复核。 |

根因不明、同一修复连续失败 2 次，或涉及金额、币种、精度、余额、关系匹配、数据写入、SQLite/PostgreSQL、迁移、权限、敏感数据、外部回调、公共 API/CLI/SDK、路由、新依赖或需求解释变化时，升级为 A 类。

## OpenSpec 事实源与回写

- `project-context.md` 定义工程原则；`specs/<capability>/spec.md` 定义当前行为；`changes/<name>/` 定义未完成变更；`changes/archive/YYYY-MM-DD-<name>/` 保留完成变更的审计记录。
- `proposal.md` 记录价值、范围、非目标和验收，delta spec 记录行为变化，`design.md` 记录架构、数据流、接口、风险、部署和 UI 策略，`tasks.md` 记录阶段、实施、审查、验证与发布准备。
- 需求、领域语义、架构、数据库、接口或风险发生变化时，先更新相应 artifact 再实施。归档前同步 delta；纯实现细节只更新 design、tasks 或代码，纯工具或文档变更使用 `skip_specs: true`。
- 新能力使用 `$openspec-propose`，当前 active change 变更范围、验收或财务语义时使用 `$openspec-update-change`。`MODIFIED` delta requirement 必须包含完整更新后的 requirement 和场景。

## 八阶段强度矩阵

所有 A/B/C 变更按「思考、计划、任务拆分与一致性、构建、审查、测试与 QA、发布、反思」组织；每个阶段在 `tasks.md` 记录动作、结论或不适用理由。B/C 可以合并相邻轻量动作，但必须保留八阶段、测试先行、审查、验证证据与外部写授权。发现新需求、架构决策、规格缺口或阻断性 finding 时，Flow-Back 到最近正确的阶段和 artifact。

| 阶段 | A：完整变更 | B：轻量变更 | C：局部缺陷修复 |
|------|-------------|-------------|-----------------|
| 1. 思考 | 阅读项目上下文、主规格、active changes、词表、代码和测试；使用 `$openspec-explore`；有产品取舍时完成产品探讨。 | 阅读受影响规格、代码和测试，记录目标、非目标、风险与不升级理由。 | 确认根因、既定行为和复现路径；根因不明则升级 A。 |
| 2. 计划 | 完整 proposal、delta spec、design；覆盖场景、失败模式、兼容、回滚与适用专项策略；UI 先完成原型。 | 小型 change 记录验收、受影响文件、实现选择和风险；新架构决策补充 design。 | 记录缺陷、预期行为、最小修复边界和非目标；出现取舍或需求解释则升级。 |
| 3. 任务拆分与一致性 | 任务覆盖每项 requirement、场景、失败测试、实施、审查和验证，并检查 artifacts 一致性。 | 覆盖验收、失败测试、最小实现、范围复核和验证。 | 先安排失败回归测试，再安排最小修复、复核和验证。 |
| 4. 构建 | 在独立 feature 分支或 worktree 中按 tasks 实施；每项测试先失败再转绿。 | 在隔离 worktree 小步实施；先失败测试再转绿。 | 以失败回归测试驱动最小修复，只恢复既定行为。 |
| 5. 审查 | 产品/范围、工程和最终 diff 必做；开发者体验、设计与安全按风险触发。 | 范围化 diff 必做，其他视角按改动触发。 | 复核根因、修复边界与回归风险。 |
| 6. 测试与 QA | OpenSpec 校验、完整回归、相称构建和全部适用专项检查。 | 受影响测试、相称构建、`git diff --check` 和适用专项检查。 | 新增回归测试、受影响测试、相称构建和 `git diff --check`。 |
| 7. 发布 | 记录发布准备、回滚和观察项。 | 记录交付证据；发布时补充相称回滚信息。 | 记录验证证据；发布时升级到相称强度。 |
| 8. 反思 | 在里程碑、发布或重复失败后沉淀改进。 | 出现可复用决策、风险或重复问题时沉淀。 | 记录防复发测试或规则；重复问题触发升级。 |

## 审查与验证证据

- 产品/范围复核检查用户、优先级、范围和成功标准；工程复核检查边界、数据流、失败模式、兼容、回滚、测试和复杂度；设计复核检查核心任务、信息架构、状态、无障碍和响应式；开发者体验复核检查 API/CLI/SDK 的可发现性、一致性、错误、示例和迁移成本；安全复核检查资产、信任边界、权限、输入、日志、依赖和外部回调。最终 diff 复核检查 artifact 偏离、遗漏测试和回归。
- 审查由独立复核上下文、人工或可用辅助工具完成。每次在正式 artifact 记录范围、覆盖风险、按严重级别排序的 finding、采纳/拒绝或延期理由及回写位置；阻断性 finding 修复后重新复核。
- UI 改动在最终 UI 形成后必须调用 Hallmark 的 `audit` 技能动作审查 `<target>`；在 `tasks.md` 记录目标、输出、finding 和结论。所有 critical 与 major finding 修复后重新调用 audit；无 finding 同样记录范围和结论。
- 执行变更的 AI 从 artifacts、改动和当前项目命令确定适用检查。存储行为变更以同一 Application Service 和用户合同在 SQLite、真实 PostgreSQL 运行契约矩阵；Web 行为或交互变更运行 Vitest、构建、Playwright 和生产预览，覆盖主流程、错误/空状态、键盘和相关响应式宽度；性能与安全风险运行专项检查。
- 在 `tasks.md` 记录实际命令、结果、当前 `HEAD`、比较基线、执行时间、未解决风险和审查结论。未运行项必须记录不适用原因、残余风险与准确补跑条件；`HEAD`、目标分支或依赖变化后重跑受影响验证。

## A 类 UI 原型门禁

1. 新增或重大调整用户可见页面、视图、交互流程、信息架构、视觉层级或响应式行为时，在第 2 阶段以 `$hallmark` 设计 UI 原型；不可见实现、文案错字和等值视觉 token 替换不触发。
2. 进入第 3 阶段前，在当前 change 创建 `prototype/index.html`。原型可直接打开并表达核心流程，可引用同目录本地资源，不依赖生产路由、后端、凭据或网络服务。
3. 原型覆盖核心任务、真实或明确标注的占位内容、正常/空/错误状态及适用的焦点、禁用、加载、成功状态；在 320、375、414、768 px 宽度检查无横向滚动和文本溢出。
4. `design.md` 记录 Hallmark 预检、设计选择、原型路径、适用状态、响应式检查与审查结论；原型样式顶部保留 Hallmark 设计与自检标记。第 5 阶段以原型复核设计，并 audit 最终 UI。
5. UI 范围、核心任务、信息架构或关键交互改变时，Flow-Back 更新原型和 `design.md` 后再实施。

## UI 设计必须约束

- 详细规则唯一维护于 [`docs/ui-design-rules.md`](docs/ui-design-rules.md)。任何 UI 设计、原型、实现或审查开始前，必须先读取该文件；违反其中任一硬性约束不得进入实现或 PR。
- UI 必须先按用户任务提交信息架构和“将删除的冗余信息”，重大 UI 变更必须先有原型并取得用户确认；确认后才能实现，范围变化必须回写原型和设计记录。
- 用户可见文案不得暴露数据库、领域模型、投影、服务边界或实现细节；正常状态帮助文字最多 2 处、每处不超过 16 个汉字，不得添加教学区、重复说明或卡片套卡片。
- 桌面端单项任务表单最大宽度为 720 px；移动端单列且无横向滚动。适用页面必须检查 320/375/414/768/1440/390 px，其中 1440 px 和 390 px 必须截图审查。
- 实现后必须做可见文字必要性自检、范围化术语搜索和 Hallmark `audit`；结果与 finding 必须回写对应 OpenSpec `tasks.md`。

## 工程原则与外部写

财务正确性、可审计性、测试覆盖、数据库等价行为、边界与安全的详细硬约束以 `openspec/project-context.md` 为唯一来源；变更必须按其风险要求执行。

### 开发节奏：小步提交并推送

- 默认按可验证、范围明确的小步推进代码、测试、规格、文档、配置和工作流规则；每完成一个语义聚焦的小步，就在相称检查通过后尽快提交并推送，不得把多个已完成的小步长期堆在未提交工作树中。
- 只有在用户明确授权当前仓库和目标分支的提交/推送后，执行者才可以进行对应的 `commit` 和 `push`；每次提交前必须检查 `git status`、`git diff --check` 和受影响验证。
- 该授权仅覆盖同一仓库、同一目标分支的提交和推送；目标改变、用户撤回授权，或需要 PR、合并、部署等其他外部写入时，必须重新确认。范围外改动必须拆分或请求确认，不得静默一起提交。

本地提交、推送、创建 PR、合并、部署、流量切换、回滚及第三方资源、外部文档或公告写入，都需要用户明确授权具体动作与目标；上面同一工作流的授权可覆盖后续小步提交和推送，但不扩大到其他外部写入。未获授权时保留工作树与验证证据；`openspec archive` 只整理仓库内记录，不代表发布授权。

删除或覆盖前确认精确目标，优先可恢复操作；范围外重构、未知根因和新增数据、权限或迁移风险先调查并回写结论。
