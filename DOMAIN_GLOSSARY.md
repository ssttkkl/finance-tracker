# 领域统一术语

本词表记录项目中需要统一使用的业务语言。代码标识符、数据库字段、CLI 参数、枚举值和协议值保持
原样；文档、注释和程序文案应根据目标读者使用下列规范术语。词条后的 `(new)` 表示本轮扫描新增。

## 规格与变更管理

| 规范术语 | 定义 | 应避免的别名 |
|----------|------|--------------|
| **OpenSpec 主规格** (new) | 位于 `openspec/specs/`、描述当前系统可观察行为并作为后续变更基线的规格文件。 | 当前 spec、需求文档、实现说明 |
| **OpenSpec 变更** (new) | 位于 `openspec/changes/`、集中记录一次行为调整的提案、delta 规格、设计和任务的工作单元。 | feature 目录、临时计划、聊天记录 |
| **delta 规格** (new) | 只描述相对于 OpenSpec 主规格新增、修改、移除或重命名的需求与场景。 | 全量 spec、补丁文档、复制版需求 |
| **变更归档** (new) | 已完成变更在 `openspec/changes/archive/` 中保留的审计历史记录；归档后的更正必须显式标注，且不能伪造完成时间。 | 删除的 feature、过期计划、临时备份 |
| **项目上下文** (new) | 约束财务正确性、数据安全、测试证据和发布授权等跨变更工程原则的 `openspec/project-context.md`。 | 工具配置、单个 feature 的 plan、聊天约定 |
| **八阶段流程** (new) | 所有 OpenSpec 变更共享的思考、计划、任务拆分与一致性、构建、审查、测试与 QA、发布、反思工作骨架。 | 仅 A 类流程、线性清单、可选步骤集合 |
| **阶段强度矩阵** (new) | 按 A/B/C 等级规定八阶段各自最低动作、证据和复核深度的规则表。 | 三套无关流程、验证脚本参数表 |
| **UI 原型** (new) | A 类 UI 变更在任务拆分前产出的可直接打开 HTML demo，用于表达核心任务、状态、响应式行为和设计审查结论。 | 生产页面、静态截图、未标注的线框图 |
| **UI 审计** (new) | 对 A/B/C UI 变更的最终页面、组件或 HTML demo 调用 Hallmark 的 `audit` 技能动作，并记录按严重级别排序的视觉与交互 finding。 | 只看截图、只审计原型而不审计最终 UI、泛泛的“看起来没问题” |

## 账本与账户

| 规范术语 | 定义 | 应避免的别名 |
|----------|------|--------------|
| **工作区** (new) | 隔离一组账户、账本记录、交易关系和估值数据的业务边界，对应内部标识 `workspace`。 | 租户（未讨论租户模型时）、账套空间 |
| **工作区成员** (new) | 与某一工作区关联并获分配访问角色的已注册用户。 | 工作区用户、团队成员（未说明工作区时） |
| **工作区角色** (new) | 工作区成员的权限等级，取值为 `admin`、`editor` 或 `viewer`。 | 用户等级、权限组（未说明边界时） |
| **工作区邀请** (new) | 由工作区管理员创建、指定加入角色且限时一次有效的加入凭据。 | 邀请码、注册链接、成员申请 |
| **工作区删除** (new) | 工作区管理员输入当前工作区名称确认后永久移除该工作区及其成员、邀请和全部账本数据的管理操作。 | 工作区移除、注销工作区、清空工作区 |
| **账户** (new) | 用来分别记录现金、负债、债权或投资持仓的账本对象，账户名称在工作区内唯一。 | 钱包（非产品名称时）、`account`（中文文案中） |
| **账户支持币种** (new) | 账户允许新建、编辑和导入的币种代码集合；同一账户可以支持多个币种，保存于 `accounts.currencies`，不是余额本身，也不设置主币种或默认币种。本期由数据库或迁移脚本提供，不在流水页面管理。 | 账户本币、账户默认币种、币种口袋、`base_currencies`（当前账户配置语义中） |
| **分币种余额** (new) | 同一账户按币种分别记录的余额集合。 | 币种口袋、余额口袋、`balance pocket`、`currency pocket` |
| **账本记录** (new) | 已进入账本并可参与查询、审计或计算的业务记录，是面向用户描述内部 `Formal Fact` 的总称。 | 正式事实、`fact`（面向普通用户时） |
| **现金流水** (new) | 记录某个账户中一笔现金增减及其时间、币种、分类和来源的账本记录。 | 现金事实、现金 `fact`、现金交易行（面向用户时） |
| **投资事件** (updated) | 记录投资账户中资金供给、交易、收入、支出、冲回、认购、调整或快照的账本记录。 | 投资事实、`investment fact`（中文文案中） |
| **标的片段筛选** (new) | 以使用者输入的文本片段，在投资事件的资产标的代码中进行大小写不敏感的包含匹配的只读筛选方式；输入中的通配符字符只按普通文本解释。 | 精确标的筛选、通配符搜索、全文搜索 |
| **余额校准** (new) | 在指定时点把账户某一币种的余额确认为已知正确值。 | 余额核对、余额校正、余额 `checkin`（中文文案中） |
| **持仓校准** (new) | 在指定时点把某一标的的持有数量和成本信息确认为已知正确值。 | 持仓核对、持仓校正、持仓 `checkin`（中文文案中） |

## 导入与溯源

| 规范术语 | 定义 | 应避免的别名 |
|----------|------|--------------|
| **导入渠道** (new) | 标识账本记录由哪一种账单、券商文件或解析流程产生，对应 `source_type`。 | 来源类型、渠道 `kind`、`source type`（中文文案中） |
| **业务行标识** (updated) | 数据源为一条业务记录提供或由可审计来源字段确定性生成的稳定标识，对应 `record_id`；它不依赖最终展示的 `note`。 | 业务行身份、行 `identity`、裸 `record id`（中文文案中） |
| **来源行快照** (updated) | 导入时随账本记录保存的原始账单业务行的完整列名和值，对应 `source_payload`；其中不得混入解析、映射或关系处理产生的数据。 | 原始 `payload`、行内溯源、原始记录（容易误指已删除的 `RawRecord`） |
| **关系派生元数据** (new) | 由来源转换或关系规划产生、用于识别退款角色和结构化配对证据的字段集合，对应 `relation_metadata`；它与不可变的**来源行快照**分开保存。 | 来源快照字段、原始 `payload`、关系备注 |
| **来源说明** (new) | 投资事件中面向使用者展示的来源原始描述、备注、动作或旗标，对应 `note`；来源未提供可读说明时为空，不承载分类、幂等身份、技术标识或派生费用。 | 系统备注、交易 ID、费用说明、分类依据 |
| **来源账户身份** (new) | 账单来源为本方资金账户提供的稳定账户、卡片、支付产品或账单档案标识；导入时先按该身份归组，再由使用者映射到当前工作区账户。它不表示交易对方，也不能从备注或商户名称猜测。 | 文件账户、支付方式账户、对方账户、账户名称猜测 |
| **支付组成项** (new) | 支付账单在一条 `收/付款方式` 中用 `&` 连接的账户或优惠成分；账户分期是账户的修饰，红包、立减、优惠、抵扣和券是不产生账户的附加项。 | 组合账户、优惠账户、分摊流水 |
| **无法唯一归属的组合支付** (new) | 一条账单同时包含多个真实资金账户但只提供合计金额、无法安全拆分到各账户的来源支付组成；该业务行标记为无法识别并跳过，只有整份文件没有可导入业务行时才阻断导入，绝不猜测账户。 | 主账户猜测、多账户合并、虚拟账户 |
| **账户别名** (new) | 使用者显式登记并绑定到工作区账户的完整账号或卡号尾号，对应 `account_aliases` 中的 `account_identifier` 或 `card_tail`；它可以为来源账户映射和关系匹配提供严格证据，但不能代替使用者确认映射。 | 账户映射、模糊名称、自动账户规则 |
| **账户映射建议** (new) | 系统依据当前工作区内既有账单账户映射或唯一账户别名，为一个来源账户身份给出的非最终预选；冲突、失效或证据不足时必须保持未选择。 | 自动映射、默认账户、账户猜测 |
| **账户映射确认** (new) | 使用者在导入会话中把每个来源账户身份明确选择到当前工作区账户的决定；即使所有选项都已预选，也必须完成确认后才能进入导入预览。 | 自动确认、跳过映射、文件级账户选择 |
| **账单账户映射** (new) | 当前工作区内，将导入渠道和规范来源账户身份持久绑定到一个账户的已确认记录；它只在最终确认导入成功时写入，供后续导入生成账户映射建议，不改变既有账本记录。 | `mapping.yaml` 规则、账户别名、导入批次账户 |
| **账户币种扩充草稿** (new) | 使用者已明确选择一个现有账户，但该账户尚未支持本次来源账户涉及的全部币种时，导入会话暂存的新增支持币种决定；它只在最终确认导入成功时写入账户配置。 | 自动改账户币种、币种兼容标签、立即扩充币种 |
| **新账户草稿** (new) | 使用者在当前导入会话明确选择创建的新账户及其名称、类型和来源币种；它在最终确认导入前只存在于会话中。 | 自动创建账户、预创建账户、临时账户 |
| **共享新账户草稿** (new) | 当前导入会话内可被多个来源账户身份共同引用的一份新账户草稿；最终确认时只创建一个系统账户并保存多条账单账户映射。 | 草稿复制、行级新账户、重复新账户 |
| **对方账号** (updated) | 原始账单中指向本笔交易对方账户的账号、卡号、掩码账号或账户标识；原始值属于来源行快照，现金流水 `counterparty_account` 保存由该值生成且不丢失可识别部分的规范表示。 | 目标卡号、收款账号、转入卡号（未说明具体来源字段时） |
| **对方账号属性** (new) | 描述规范对方账号是完整标识、尾号、掩码表示或经严格验证重建值的有序属性集合，对应 `counterparty_account_attrs`。 | 账号类型、卡号格式、匹配器猜测 |
| **本人账户标识** (new) | 用户显式登记并绑定到账户的完整账号或卡号尾号，用于判断一条来源直接提供的对方账号是否指向该账户；不得从账户名称、备注或映射规则推断。 | 账户猜测、自动卡号、账户名称尾号 |
| **工银亚洲活期账户明细** (new) | 工银亚洲导出的 UTF-16 制表符 CSV 账单，交易表以 `序號` 开头，并包含独立的无标题时间列、收支金额、对方账号和对方户名。 | 工银亚洲流水、`currentaccounthistory` 文件（未说明账单语义时） |
| **工银亚洲规范账号** (updated) | 将工银亚洲完整币种子账号的末位币种区分位标准化为 `0` 后得到的同长度账户标识；同一规范账号的子账号归入同一账本账户。 | 工银亚洲主账号、去末位账号、币种账户、完整子账号 |
| **扩展业务信息** (new) | 投资事件中未提升为正式字段、但仍具有业务意义的补充信息，对应 residual `payload`。 | `residual`、剩余 `payload`、来源快照 |
| **流水类型** (updated) | 表达单条现金流水自身业务性质的统一分类，对应 `record_type`；导入时由来源语义确定，使用者也可以在流水编辑界面校准。 | 收支方向、账户类型、关联类型、个人转账类型 |
| **业务细分** (new) | 在部分流水类型下进一步表达资金移动语义的可选分类，对应 `record_subtype`；界面只在适用时展示。 | 关系类型、账单来源类型、技术子类型 |
| **流水方向** (new) | 由现金流水当前金额符号确定的汇总方向：负数为支出、正数为收入、零金额没有收入或支出方向；它不替代流水类型。 | 收支类型、消费类型、`record_type` |
| **零金额流水** (new) | 金额为零、因此不增加收入或支出汇总，但仍保留使用者选择或来源确定的流水类型，并可保存、查看、编辑和删除的现金流水。 | 中性类型、无效流水、零金额类型、余额调整类型（用户界面中） |
| **普通转账子类型** (new) | 表示非明确跨境汇款、非同一规范账户调拨的标准转账，对应 `record_subtype=ordinary_transfer`。 | 普通流水、默认转账、空子类型 |
| **跨境汇款子类型** (new) | 表示来源明确为跨境汇款通道的资金转移，对应 `record_subtype=cross_border_remittance`；是否跨币种由已配对两端币种决定。 | 跨境换汇、跨币种转账 |
| **内部账户调拨子类型** (new) | 表示来源直接对方账号规范化后指向同一账本账户的资金移动，对应 `record_subtype=internal_account_transfer`。 | 子账户转账、内部换汇、账户内流水 |
| **还款记录类型** (new) | 表示现金流水本身是偿还信用账户或贷款账户的业务记录；它独立于转入和转出，不因金额方向改变语义，对应 `record_type=repayment`。 | 转账收入、转账支出、还款转账、信用借还（作为统一类型名时） |
| **消费退款记录类型** (updated) | 表示一笔消费已经退回全部或部分金额，允许正向现金流水进入消费退款关系，对应 `record_type=refund`；个人转账、红包和群收款的退回不属于此类型。 | 撤销记录、冲正记录、泛化的退款类型 |
| **转账退回记录类型** (new) | 表示个人转账、红包或群收款被退回的来源业务记录，对应 `record_type=transfer_reversal`；它不进入消费退款、支付镜像或普通转账配对。 | P2P 退款、转账退款、转账撤销、消费退款 |
| **撤销记录类型** (updated) | 表示来源明确标记为撤销、冲正的非转账现金流水，对应 `record_type=reversal`，不进入消费退款关系。 | 退款记录、消费退款、P2P 退回、泛化的冲销 |
| **提现出账记录类型** (updated) | 表示从支付平台或银行账户提取现金、资金离开该账户的来源业务记录，对应 `record_type=withdrawal_out`。 | 转账支出、提现转账、泛化的转出 |
| **提现入账记录类型** (new) | 表示提现资金进入银行或其他现金账户的来源业务记录，对应 `record_type=withdrawal_in`。 | 普通转账收入、提现回执（未说明方向时） |
| **来源摘要** (new) | 银行账单直接提供的交易类型字段，例如工行的 `消费` 或 `退货`，用于表达来源语义而不是交易对方。 | 摘要备注、对手方摘要、`summary`（中文说明中） |
| **正式退款信号** (new) | 经过来源和字段值严格校验、允许关系匹配把一笔正金额现金流水作为退款候选的结构化来源字段。 | 退款关键词、退款猜测、`refund signal`（中文说明中） |

## 账单导出与设备适配

| 规范术语 | 定义 | 应避免的别名 |
|----------|------|--------------|
| **支付账单导出** (new) | 面向微信支付、支付宝及后续支付渠道的账单申请、生成状态检查、下载和保存统一入口，对外业务名称为 `bill-export`。 | 支付宝账单导出 Skill、微信账单导出 Skill、账单抓取 |
| **渠道流程** (new) | 某一支付渠道在应用内从申请到账单文件保存的完整业务操作、参数映射、状态判断和恢复规则。 | 设备适配文件、控制后端、通用点击脚本 |
| **控制后端** (new) | 只描述如何借助 Computer Use、iPhone 镜像、ADB 等工具观察和操作设备界面的实现手段，不包含支付渠道的业务路径。 | 渠道流程、支付宝后端、微信后端 |
| **交易流水证明** (new) | 支付宝按指定时间范围生成、可下载保存的账单证明文件，是支付宝渠道流程的导出对象。 | 普通支付宝账单、支付明细截图、CSV 账单（未明确格式时） |
| **认证接管** (new) | 自动化后端到达敏感认证页面后暂停，由使用者在设备上完成认证并明确告知可以继续的交接状态。 | 代输密码、自动登录、跳过认证 |
| **证明生成状态** (new) | 对申请已提交、等待生成、已生成或未确认等状态的可见业务判断，不等同于下载或保存成功。 | 下载状态、申请状态（未说明阶段时）、消息状态 |

## 投资交易

| 规范术语 | 定义 | 应避免的别名 |
|----------|------|--------------|
| **投资事件记录类型** (updated) | 投资事件对外表达的经济事实类型，对应 `investment_events.record_type`；它取代原字段名 `action`，方向由付出资产与换入资产表达。 | 投资动作、入金类型、出金类型、`action`（面向新文档和接口时） |
| **投资事件记录子类型** (updated) | 在投资事件记录类型下进一步标识经济事实范围或原因的规范语义，对应 `investment_events.record_subtype`。 | 券商类型、账单类型、`action subtype` |
| **资金供给** (new) | 投资账户与外部账户或同一机构子账户之间的资金移动，对应 `funding`；方向由现金部分位于付出资产或换入资产确定。 | 入金、出金、充值、提现（作为一级类型时） |
| **外部出入金** (updated) | 在收支账户与投资账户之间实际转移的资金供给，对应 `funding(external)`；它是现金—投资资金调拨关系唯一可配对的投资事件。 | 现金收入、现金消费、泛化的充值或提现 |
| **投资账户内部调拨** (updated) | 资金仅在同一投资机构或其子账户之间移动的资金供给，对应 `funding(subaccount)`，不得与收支账户流水配对。 | 外部入金、银行转账、子账户出入金 |
| **投资收入** (new) | 因投资持有或平台奖励产生的收益，对应 `income`，包括现金股息、股票股息、利息和奖励。 | 入金、现金调整、投资收益流水（未说明经济事实时） |
| **投资支出** (new) | 因投资交易、持有或账户服务产生的费用，对应 `expense`，包括佣金、税、利息、手续费和罚息。 | 出金、扣款、费用流水（未说明经济事实时） |
| **冲回** (new) | 对已产生的投资支出或出金进行返还、撤销或抵减的经济事实，对应 `reversal`；它不等同于一笔新收入或入金。 | 退款入金、费用收入、泛化的退款 |
| **费用冲回子类型** (new) | 标识被返还或撤销的原投资支出类别；税、利息、佣金、手续费和罚息分别使用对应的 `expense_*` 子类型，不得写为外部出入金。 | 平台返现、退款入金、费用收入 |
| **认购** (new) | 新股或其他投资产品认购的扣款与退款经济事实，对应 `subscription`。 | IPO 入金、认购出入金 |
| **投资调整** (new) | 无法作为交易、资金供给、收入、支出、冲回、认购或快照解释的投资账户变动，对应 `adjustment`。 | 外部出入金、未知入金、现金修正 |
| **投资快照** (new) | 在指定时点确认现金余额或持仓数量的投资事件，对应 `snapshot`。 | `checkin`、余额校准事件（未说明记录类型时） |
| **记录基准** (new) | 周期表现遇到投资快照时，以该账户和标的最后一条快照的发生时间与确认数量开始计算的可观测起点；它不等同于周期起点，也不能据此倒推出更早的真实持仓或完整收益。 | 快照倒放、补全期初持仓、真实期初（未知时） |
| **账单产品档案** (new) | 由账单正文直接识别的账户产品类别，用于在受控重建时将来源文件路由到对应的本地投资账户。 | 文件名类型、账单后缀、账户猜测 |
| **现金部分** | 一笔投资事件中表示现金金额的组成部分；手续费是否包含在内由对应数据源的费用合同决定。 | 现金腿、`cash leg`（面向用户的中文说明中） |
| **交易总费用** (new) | 一笔投资交易由来源明确列出的佣金、税费和过户费等全部费用之和；在费用可从净额现金部分安全拆出时，对应 `commission` 及其 `commission_asset`。 | 仅佣金、费用备注、隐含手续费 |
| **证券部分** | 一笔投资事件中表示证券标的及其数量的组成部分。 | 证券腿、`security leg`（面向用户的中文说明中） |
| **付出资产** | `swap` 中被交付或扣减的资产，对应 `from_ticker` 和 `from_amount`。 | 支出腿、from 腿 |
| **换入资产** | `swap` 中收到或增加的资产，对应 `to_ticker` 和 `to_amount`。 | 收入腿、to 腿 |
| **资产流出** (new) | 投资事件使某项资产数量减少的方向，投资事件界面简写为“流出”；它不等同于投资支出。 | 付出（用户界面中）、支出（未表达费用时）、资产减少腿 |
| **资产流入** (new) | 投资事件使某项资产数量增加的方向，投资事件界面简写为“流入”；它不等同于投资收入。 | 换入（用户界面中）、收入（未表达收益时）、资产增加腿 |

## 关系匹配

| 规范术语 | 定义 | 应避免的别名 |
|----------|------|--------------|
| **关系审查列表** (new) | 集中展示待人工判断、确认或驳回的交易关系候选项。 | 审查收件箱、`Review Inbox`（中文文案中） |
| **关联流水** (updated) | 面向普通使用者的操作用语，表示同一笔收支详情下共同展示的另一条或多条流水；它们仍各自保留并可单独查看。 | 关联记录、关联记录合并、关系投影、端点合并、配对关系 |
| **关联类型** (new) | 使用者在添加关联流水时选择的业务关系，例如同一笔支付、退款、个人转账或银证转账；它不属于任何单条流水的流水类型。 | 记录类型、投影类型、关系 `kind`（用户界面中） |
| **取消关联** (new) | 将选中的关联流水从当前收支详情中移出并保留为独立流水；剩余关联流水继续保持现有组合，并保留当前的人工拒绝决定，避免系统自动再次推荐同一组流水。 | 删除流水、删除历史、解除投影 |
| **关联忽略决定** (new) | 使用者取消关联后保留的当前拒绝状态，用于阻止自动扫描再次建立同一组流水的关系；它不是关系版本或操作日志。 | 关系历史、关系软删除、审计记录 |
| **同笔支付关系** (new) | 表示两个渠道中的两条现金流水实际记录了同一笔支付，对应 `payment_mirror`。 | 支付镜像、镜像支付、`payment mirror`（中文文案中） |
| **退款候选事件组** (new) | 将已确认 `payment_mirror` 的多条来源流水折叠为一笔经济事件，作为退款匹配中的一个候选。 | 镜像候选、候选流水组、重复候选 |
| **最近且唯一候选** (new) | 在最高证据等级仍有多个退款候选时，退款与消费时间差最小且只有一个的候选事件；部分退款和全额退款均可使用。 | 最近退款、时间最近就配对、随机最近行 |
| **确定性同笔支付配对** (new) | 对字段完全一致且两侧数量相等的一组同笔支付候选，按各自发生时间和稳定记录 ID 排序后逐项配对的自动确认规则；一个端点只能被一个渠道对占用。 | 最近候选猜测、交叉镜像、随机配对 |
| **转账配对关系** (updated) | 表示两条现金流水分别是同一次资金移动的转出和转入，对应 `transfer_pair`；具体业务含义由关联类型和关系子类型确定。 | 转账腿、`transfer pair`（中文文案中）、单条流水类型 |
| **个人转账关系** (new) | 由一条负金额转出流水和一条正金额转入流水确认形成的关联，不作为单条流水的流水类型，对应普通 `transfer_pair`。 | 个人转账记录类型、内部转账流水、消费类型 |
| **现金—投资资金调拨关系** (updated) | 表示一条收支账户现金流水与一条 `funding(external)` 投资事件属于同一次资金转移的可审计关系。 | 银证腿、跨账本镜像、泛化的转账配对 |
| **机构名称确认信号** (new) | 由投资导入渠道对应的受控券商机构名称提供的资金调拨附加证据；它不包含收款账号、本人名称或原始备注。 | 账号硬编码、任意对手方文本、券商猜测 |
| **个人购汇关系** (new) | 表示已明确分类的购汇转出与购汇转入属于同一次个人购汇的转账配对关系，对应 `transfer_pair(currency_exchange)`。 | 购汇收入、购汇消费、换汇流水合并 |
| **购汇转出** (new) | 个人购汇中以原币种扣减资金的负额现金流水，对应 `fx_out`。 | 购汇消费、购汇出账腿 |
| **购汇转入** (new) | 个人购汇中以换入币种增加资金的正额现金流水，对应 `fx_in`。 | 购汇收入、购汇入账腿 |
| **跨境汇款** (new) | 通过跨境汇款通道发生的同币种资金转移，按方向对应 `transfer_out` 或 `transfer_in`；只有来源明确表达购汇或汇兑时才属于换汇。 | 跨境换汇、`fx_out`、`fx_in` |
| **跨币种汇款关系** (new) | 已配对的跨境汇款两端币种不同的转账关系，对应 `transfer_pair(cross_currency_remittance)`；它不改变任一流水的跨境汇款子类型。 | 自动换汇、跨境购汇 |
| **转账出账种子** (updated) | `record_type` 为负向资金移动且 `record_subtype` 为可匹配资金移动语义的现金流水；不依赖来源或账单文本。 | 任意负数流水、来源信号种子 |
| **标准化转账匹配** (new) | 只以正式记录类型、子类型、账户、规范对方账号、币种、金额、时间和本人账户别名扫描资金移动关系的规则。 | 来源专用桥接、账单文本配对 |
| **退款冲销关系** (updated) | 表示一笔退款全部或部分抵销原支出的关系，对应 `refund_offset`；两端均为零且同币种时也可作为零金额退款保留证据链。 | 退款腿、退款 `offset`（中文文案中） |
| **信用账户还款关系** (new) | 表示现金账户付款与信用账户负债减少属于同一次还款的关系，对应 `credit_repayment`。 | 信用还款 `subtype`、还款腿 |
| **待配对关系** | 已确定一笔锚点流水，但尚未唯一确定另一笔流水的待审核关系；内部状态称为 `open-leg pending`。 | 开放单腿、单腿 `pending`、`open-leg`（面向用户的中文说明中） |
| **锚点流水** | 发起关系匹配并在待配对期间保持确定的那笔流水，对应内部的 anchor fact。 | 锚点腿、anchor 腿 |
| **对侧流水** | 与锚点流水共同组成完整关系的另一笔流水，对应 `other_fact_id` 或 secondary fact。 | 对侧腿、另一腿 |

## 账本浏览

| 规范术语 | 定义 | 应避免的别名 |
|----------|------|--------------|
| **收支账本** (updated) | 展示当前工作区可见收支投影的只读账本界面，包括消费、收入和不计收支的内部转账。 | 消费账本、现金页面、消费流水页、`cash view` |
| **收支投影** (new) | 由有效现金流水和已确认关系确定性生成的派生账本结果；每条有效现金流水恰好归属一个收支投影。 | 原始流水列表、关系摘要列表、合并流水、`cash projection`（中文文案中） |
| **投影条目** (new) | 收支投影中可独立查询和审计的一项结果，无关系的现金流水也会投影为单成员条目。 | 合并条目、虚拟流水、原始条目 |
| **单源投影** (new) | 仅由一条账本记录直接形成、未采用任何已确认关系的投影条目。 | 原始记录投影、单条流水、普通投影 |
| **关系投影** (new) | 由两条或更多账本记录经已确认关系共同形成的投影条目。 | 组合投影、合并流水、关系记录 |
| **投影成员** (new) | 归属于同一投影条目的现金流水，用于说明该结果由哪些事实组成。 | 子流水、关系端点、投影来源行 |
| **展示基准流水** (new) | 系统内部为一笔收支详情选择的字段来源，用来稳定提供发生时间、账户、交易对方、对方账号、分类、备注等顶部展示字段；它不是用户可见的主次关系，不能在每次重建时重新挑选。 | 主流水、主记录、首条记录、最新记录、逐字段主来源 |
| **投影来源** (new) | 收支账本和收支详情中呈现的导入渠道集合；单源投影取自身渠道，关系投影取全部投影成员渠道的有序去重集合。 | 来源类型、来源快照、`provider` |
| **经济类型** (updated) | 表明投影条目属于消费、收入还是内部转账的业务分类；内部转账不计入收入、支出或月度汇总，但仍在收支账本中展示。 | 流水正负类型、账户类型、交易方向 |
| **内部转账子类型** (updated) | 对内部转账进一步区分普通转账、信用还款、换汇、银证转账或其他不计收支资金移动的分类；所有内部转账两端金额异号，同币种时才要求绝对金额相等，异币种时不比较绝对金额；换汇还必须使用不同币种。 | 转账关系类型、`subtype`（中文文案中） |
| **经济类型筛选树** (new) | 由当前活动投影数据集的可见投影聚合出的筛选层级：一级为经济类型，二级为该一级下实际存在的内部转账子类型。 | 前端写死的筛选项、类型枚举下拉框 |
| **银证转账** (new) | 已确认现金—投资资金调拨在收支账本中的专用展示和筛选语义，对应 `internal_transfer(bank_security_transfer)`；列表同时显示收支账户与投资账户的实际两端金额。 | 投资收入、投资支出、零金额转账、泛化的个人转账 |
| **组成方式** (new) | 表明投影条目由单条记录、同笔支付归并、退款冲销或其组合形成的只读筛选维度。 | 关系状态筛选、关系类型筛选、合并方式 |
| **投影版本** (new) | 标识工作区当前已发布收支投影的一致性版本，分页游标只能在同一版本内继续使用。 | 缓存版本、数据版本、页面版本 |
| **关系种类不变量** (new) | 已确认关系参与投影前必须满足的端点方向、金额和币种约束，违反时必须阻断而非隐藏收支。 | 关系校验、关系格式检查 |
| **关系图互斥约束** (new) | 自动确认候选加入其完整已确认关系连通组后，`transfer_pair` 与 `refund_offset` 不得同时存在；`payment_mirror` 可与任一方共存。 | 端点互斥、所有关系都互斥、关系类型全局互斥 |
| **历史关系修复** (new) | 对经明确授权、违反当前关系规则的既有关系做可审计状态修正的运维操作；保留原始账单、关系和证据，不删除事实。 | 清库、删脏数据、覆盖旧关系 |
| **来源摘要** (new) | 银行账单提供的交易语义字段，例如“转账”“消费”或“退款”；在标准导入行中保留为 `summary`，独立于交易场所和备注。 | 交易场所、备注、交易对方、来源行摘要（未说明字段时） |
| **投影来源摘要** (new) | 用于确认暂存投影仍对应同一组投影与证据输入的规范摘要，覆盖全部会影响派生结果的有效流水和已确认关系字段。 | 数据指纹、构建哈希（未说明用途时） |
| **投资账本视图** (new) | 按投资账户展示投资事件、持仓与持仓估值的只读视图。 | 投资页面、投资流水页、`investment view` |
| **关系摘要** (updated) | 投影条目采用的已确认关系类型和数量的简要展示，用于说明其组成方式。 | 关系标签、全部关系状态汇总、关系概况（未说明内容时） |
| **收支详情** (updated) | 供使用者核对和编辑一笔收支结果的默认详情，展示金额、币种、交易对方、对方账号、时间、账户、流水类型、适用的业务细分、分类、备注和来源；有关联流水时在同一抽屉中列出它们，不暴露内部主次关系。 | 投影结果、主记录详情、原始详情、证据详情 |
| **编辑收支详情** (new) | 在现有信息抽屉内直接切换的编辑状态；使用者编辑看到的整笔收支详情，系统内部再把修改写入对应的实际流水并重建派生结果。 | 编辑流水、编辑投影、独立编辑页 |
| **关联流水** (updated) | 收支详情下共同展示的其他实际流水，以各自的金额、时间、账户、交易对方、对方账号、流水类型、来源等可读字段呈现，不向使用者暴露内部角色。 | 关联记录、成员流水、投影成员、关系端点 |
| **模态信息抽屉** (new) | 覆盖在收支账本上方、通过遮罩隔离背景并展示收支详情的交互层；桌面从右侧滑入，窄屏占满可用宽度，查看与编辑在同一抽屉内切换。 | 证据详情、独立编辑页、固定浮层 |
| **模态证据抽屉** (new) | 覆盖在收支账本上方、通过遮罩隔离背景并展示证据详情的只读交互层；桌面从右侧滑入，窄屏占满可用宽度。 | 侧栏（指导航栏时）、固定浮层、详情面板（未说明模态语义时） |
| **稳定分页** (new) | 在筛选条件不变时，连续翻页不会重复或遗漏同一排序范围内账本记录的浏览方式。 | 翻页、页码（未说明稳定性时） |

## 收支账本管理

| 规范术语 | 定义 | 应避免的别名 |
|----------|------|--------------|
| **手工现金流水** (new) | 由使用者直接录入、而非由账单文件解析产生的一条现金流水。 | 手工账单、虚拟流水、临时记录 |
| **流水记录** (new) | 面向普通使用者展示一条收支记录的产品用语，包含手工录入或账单导入的现金流水。 | 现金事实、投影条目、现金交易行 |
| **实际流水校准** (new) | 直接修改导入或手工现金流水当前有效业务字段，使账本结果符合使用者确认的实际情况；不创建修正版流水。 | 修改投影、修正流水、覆盖来源记录 |
| **来源保护** (updated) | 只保护导入渠道、业务行标识和来源行快照等来源字段不被用户编辑；实际业务字段仍可通过明确的校准操作修改。 | 锁定记录、整行只读、来源锁 |
| **校准覆盖** (new) | 记录某个实际流水字段当前采用用户值而非最近导入来源值的状态，对应 `manual_overrides`。 | 手工版本、修订版本、覆盖记录 |
| **账单原值** (updated) | 最近一次导入账单为某个流水字段提供的来源值，仅供导入合并逻辑使用，用户界面不展示。 | 原始值、来源标准值、历史值 |
| **来源指纹** (new) | 由一次导入来源行确定性生成、用于判断来源内容是否变化的摘要；它不替代业务行标识。 | 文件指纹、记录版本、来源 ID |
| **实际删除** (new) | 使实际现金流水从当前账本、余额和收支投影中消失的删除语义；再次导入相同业务行时可以重新发布。 | 软删除、假删除、删除恢复 |
| **现金流水信息抽屉** (updated) | 复用既有信息抽屉外壳，默认以查看态展示收支详情；点击“编辑”后在同一抽屉切换为**编辑收支详情**，来源字段始终只读，关联流水也在当前抽屉管理。 | 独立编辑页、另开编辑页、详情页和编辑页分离 |
| **导入会话** (new) | 从选择账单文件到预览、字段映射、冲突处理和结果确认的一次性用户操作流程，不是持久化账本实体。 | 导入批次、文件档案、导入记录 |
| **导入预览行** (new) | 导入会话中展示解析结果、幂等判断和字段映射状态的暂存行，尚未成为账本记录。 | 原始行、待入账事实、导入批次行 |
| **重复候选** (new) | 导入预览行根据导入渠道和业务行标识判断为已存在或可能重复的结果；它可以触发来源合并，但不产生第二条当前流水。 | 重复账单、重复流水、幂等记录 |
| **导入结果摘要** (new) | 导入会话结束时对新增、跳过、冲突、失败和需人工处理行数的可复核汇总。 | 导入成功数、批次统计、文件结果 |
| **导入处理页面** (new) | 承载选择文件、导入预览、关系配对和确认导入的独立页面，不在收支账本抽屉内完成。 | 导入弹窗、导入抽屉、导入向导（未说明页面形态时） |
| **账单密码** (new) | 使用者为受保护的账单 PDF 提供、仅用于本次解析的解密凭据，不属于账本数据或导入记录。 | PDF 密钥、文件密码（未说明用途时）、账户密码 |
| **自动识别渠道** (new) | 系统根据账单文件结构和已声明解析器确定唯一导入渠道的结果，不要求使用者先手选渠道。 | 文件类型、账单来源（未说明识别动作时）、自动匹配 |
| **配对建议** (new) | 导入预览行与现有或同批现金流水之间由关系规则生成的可核对候选，分为自动配对和待手动配对。 | 关系候选、匹配结果（未说明状态时）、关联推荐 |
| **待手动配对** (new) | 关系规则无法安全自动确认、但可以交由使用者选择对侧流水的待审核关系；确认导入时可以暂不处理。 | 未完成匹配、手动关系、人工关系（未说明待审核状态时） |
| **拒绝配对** (new) | 使用者在导入会话中明确不接受一条关系建议的决定；它不会创建已确认关联，并可在当前会话中撤销。 | 删除关系、删除流水、拒绝流水 |

## 收支分类

| 规范术语 | 定义 | 应避免的别名 |
|----------|------|--------------|
| **收支分类** (new) | 使用者从当前工作区的分类目录中选择、用于整理一笔收支详情的树状标签；它不决定流水方向、流水类型、业务细分或经济类型。 | 流水类型、经济类型、来源分类、`record_type` |
| **分类目录** (new) | 当前工作区支持的收支分类树，保存分类名称、分类描述、父子关系和同级顺序，最多包含 5 层。 | 分类枚举、前端分类常量、AI 分类结果 |
| **分类描述** (new) | 使用者为一个收支分类填写的可选说明，用于帮助人工选择，并可在未来作为 AI 分类识别的受控上下文。 | 流水备注、分类规则、AI 提示词 |
| **分类路径** (new) | 一个收支分类从顶级节点到自身的有序祖先链；选择父分类筛选时同时覆盖该路径下的全部后代分类。 | 分类名称、目录路径、文件路径 |
| **无分类** (new) | 一笔收支详情当前没有引用任何收支分类的状态，对应空 `category_id`，不是分类目录中的可编辑节点。 | 其他分类、默认分类、空分类节点 |
| **批量分类** (new) | 将使用者明确勾选的已加载收支详情原子设置为同一收支分类或无分类的操作；任一选中项失效时整批不写入。 | 全量自动分类、批量编辑所有字段、选择全部筛选结果 |

## 持仓与估值

| 规范术语 | 定义 | 应避免的别名 |
|----------|------|--------------|
| **行情报价** (new) | 某一数据源在指定时点给出的标的价格及其计价币种。 | `quote`（中文文案中）、行情价格观测值（面向用户时） |
| **报价时间** (new) | 数据源为一笔行情报价提供的、带时区的实际发生时间；不能用请求或刷新时间替代。 | 更新时间、刷新时间、请求时间 |
| **报价时段** (new) | 报价发生在该市场的盘前、常规交易、盘后或夜盘中的哪一段；无法从来源确认时为未知。 | 交易状态、市场状态（未说明具体时段时） |
| **持仓估值** (new) | 根据持有数量和可用行情报价计算出的持仓价值。 | 持仓投影、`portfolio valuation`（中文文案中） |
| **计价币种** (new) | 行情报价用来表示标的价格的币种。 | 本币、当地币种、`local currency`（中文文案中） |
| **计价币种市值** (new) | 按标的行情报价所用币种表示的持仓市值。 | 本币市值、`local market value`（中文文案中） |
| **展示币种** (updated) | 用户可输入、用于汇总或显示估值结果的目标币种，对应 `display_currency`；留空表示按原币种展示。 | 基准币种（未定义基准时）、`display currency`（中文文案中） |
| **折算市值** (new) | 将计价币种市值按可用汇率换算为展示币种后的金额。 | 展示市值、转换后市值、`converted value`（中文文案中） |
| **估值状态** (updated) | 供内部查询和诊断使用的行情或汇率可用性结果；持仓用户界面只呈现整体读取成功或失败，不呈现该状态枚举。 | 价格状态、`complete`、`partial`、`stale`（用户文案中） |
| **均摊成本** (updated) | 同一标的当前持仓的总成本除以总数量得到的单位成本；合并持仓时重新计算，不修改账本记录，用户界面使用“平均成本”。 | 平均买入价（未说明口径时）、成本均价（内部字段语义不明时） |
| **当前浮盈亏** (new) | 当前持仓市值减去持仓总成本得到的尚未实现收益或损失金额。 | 账面收益、已实现收益、估值差额（未说明是否已实现时） |
| **浮盈亏率** (new) | 当前浮盈亏除以持仓总成本绝对值得到的比例；总成本为 0 时没有可定义的比率。 | 回报率、收益率（未说明时间范围时）、涨跌幅 |
| **仓位** (new) | 单个投资标的当前市值占全部投资标的与现金当前市值之和的比例。 | 投资占比（未说明分母时）、持仓比例（未说明是否含现金时） |
| **表现周期** (updated) | 用于比较期初与期末资产并计算期间盈亏的时间范围，例如近 24 小时、本周至今或年初至今；用户界面使用“时间范围”。 | 时间筛选、统计周期、日期范围（用于事件筛选时） |
| **周期盈亏** (new) | 期末资产总额减期初资产总额并扣除外部入金、加回外部出金后得到的期间收益；买卖仍包含在资产变化中。 | 今日收益、期间涨跌、现金变化 |
| **外部出入金影响** (new) | 投资账户与工作区外部账户之间的资金流入或流出对资产总额造成的变化，不属于投资收益。 | 入金收益、充值收益、资金回报 |
| **持仓展示偏好** (new) | 只保存在当前浏览器、用于控制账户筛选、标的排序、持仓合并、展示币种和表现周期的显示设置。 | 账户配置、投资设置（未说明是否写入账本时） |
| **持仓实时流** (new) | 服务端向当前持仓页面单向发送同一展示口径下最新持仓估值快照的 SSE 连接，不是浏览器定时轮询，也不是原始行情源的逐笔订阅。 | 持仓轮询、实时价格推送 |
| **行情刷新器** (new) | 服务端常驻的只读任务，按手动刷新优先、活跃展示口径次之的顺序批量读取当前行情、历史行情和汇率，并更新内存中的最新估值快照。 | 页面请求线程、行情轮询器 |
| **行情备用源** (new) | 当首选行情数据源无法给出某一证券当前报价时，按确定优先级读取该证券报价的独立外部数据源；它只补充缺失报价，不改写已得到的首选报价。 | 数据源随机切换、价格猜测、行情缓存 |

## 数据同步

| 规范术语 | 定义 | 应避免的别名 |
|----------|------|--------------|
| **数据源** (new) | 提供账本记录、行情报价或汇率数据的外部系统或服务。 | `provider`（中文文案中）、供应商（未讨论商业主体时） |
| **连接器** (new) | 按项目约定从一种数据源读取、转换并提交数据的适配组件。 | 接头、`connector`（中文文案中） |
| **同步游标** (new) | 连接器记录的增量同步位置，用于确定下一次同步从哪里继续。 | 同步指针、`cursor`（中文文案中） |

## 概念关系

- 一个**工作区**包含多个**账户**；一个账户可以有多项**分币种余额**。
- **工作区删除**只能由当前工作区管理员发起，并在确认名称完全匹配后永久删除该工作区及其成员、邀请和全部账本数据；删除后用户切换到其他可用工作区或进入创建工作区页面。
- **现金流水**和**投资事件**都是**账本记录**，但分别表达现金变动和投资资产变动。
- 一条导入产生的账本记录由**导入渠道**和**业务行标识**共同确定幂等身份，并可携带一份
  **来源行快照**；投资事件还可携带**扩展业务信息**，两者用途不同。
- 每条现金流水在导入时都必须生成一个**标准记录类型**；**还款记录类型**独立于转入和转出，未知来源语义才使用 `other`，不把金额正负本身当作业务类型。
- **消费退款记录类型**只表示消费退款；个人转账、红包和群收款的退回属于**转账退回记录类型**，一般撤销或冲正才属于**撤销记录类型**。提现按方向分为**提现出账记录类型**和**提现入账记录类型**，不能仅因金额方向被改写成退款或普通转账。
- 一笔 `swap` 同时包含**付出资产**和**换入资产**；其中一方可以是**现金部分**，另一方可以是
  **证券部分**。
- **付出资产**和**换入资产**是内部精确合同；投资事件界面将真正发生数量变化的方向分别显示为
  **资产流出**和**资产流入**。交易通常同时显示两侧，其他事件只显示实际变化的一侧；快照显示“余额”或“持有”，不伪装成资产流动。
- 一条**待配对关系**必须有一笔**锚点流水**，但在完成配对前可以没有**对侧流水**；确认后才成为
  完整的**转账配对关系**或**退款冲销关系**。
- **转账出账种子**必须具有负向可匹配的 `record_type` 和 `record_subtype`；普通消费、退款和不适用子类型不得发起 `transfer_pair` 候选，关系层不得以来源或账单文本提升它们。
- **对方账号**与**对方账号属性**必须一同由导入边界生成；关系匹配只能按属性解释账号表示，不能根据长度、掩码符号或导入渠道重新猜测其含义。
- **来源账户身份**描述本方账户，**对方账号**描述交易对方；两者即使出现相同尾号也不能互相替代。导入先扫描来源账户身份，再让使用者完成**账户映射确认**。
- **账户映射建议**只提供预选：既有**账单账户映射**优先，其次才是唯一**账户别名**；别名冲突或目标账户失效时必须要求使用者重新选择，币种不足则形成**账户币种扩充草稿**。
- **账户映射确认**在当前导入会话中只形成待提交决定；最终确认导入成功时才持久化**账单账户映射**，并按明确选择生成**账户币种扩充草稿**或**新账户草稿**，离开页面或确认失败不得留下这些变化。
- 选择现有账户时，来源账户涉及的币种可以超出当前**账户支持币种**；系统把缺少的币种列为**账户币种扩充草稿**，不得因为币种暂未配置而阻断映射确认。
- **跨境汇款**是转账通道语义，不能据此生成**购汇转出**或**购汇转入**；只有来源明确表达购汇或汇兑时才使用 `fx_out` 或 `fx_in`。
- 每条有效**现金流水**恰好归属于一个**收支投影**；没有生效关系时，它形成只有一个
  **投影成员**的**单源投影**；采用已确认关系后，相关流水共同形成**关系投影**。
- **收支投影**只消费已确认关系。同笔支付先归并，内部资金移动再归为相应**内部转账子类型**，
  退款最后冲销原消费金额；待审核、已驳回和已取代关系不改变投影结果。
- **确定性同笔支付配对**只适用于同一渠道对、账户、交易对方、币种、金额、方向和自然日均一致，且两侧
  数量相等的候选组；两侧数量不等时保留**待审核关系**，不猜测配对。**投影成员**不能被多个同笔支付渠道对重复占用；
  出现竞争渠道对时，按规范化渠道对键稳定选择其一。
- 一个**投影条目**内部使用稳定的**展示基准流水**提供顶部描述字段；除金额、**经济类型**、组成关系和展示状态外，展示字段整体取自该流水。关系投影中的其他流水以**关联流水**说明其业务影响，单源投影不显示关联流水。
- **投影来源**不改变展示基准流水的描述字段：单源投影取该流水的**导入渠道**，关系投影取全部
  **投影成员**的导入渠道并按成员顺序去重；用户界面不展示展示基准流水的内部角色。
- **模态证据抽屉**打开时，收支账本背景不可交互；点击遮罩关闭抽屉，点击抽屉内容不会关闭，关闭后回到原证据入口。
- **收支账本**按金额确定单条流水的**流水方向**；**零金额流水**仍按自身**流水类型**在列表中展示，但不计入收入或支出。
  已确认的**个人转账关系**和**银证转账**显示实际两端金额，全额退款仍保留投影但不出现在列表中。
- **经济类型筛选树**只反映当前活动投影数据集中的可见事实；它决定可选择的类型和子类型，不改变
  **经济类型**或**内部转账子类型**本身的领域语义。
- **投影版本**更新后，旧版**稳定分页**游标失效，使用者必须从当前版本重新读取列表。
- **手工现金流水**和导入现金流水都可以进入**实际流水校准**；**来源保护**只限制来源字段，实际业务字段的当前值可以被校准，投影仍然不可直接编辑。
- **卡片流**负责浏览现金流水；点击卡片打开**现金流水信息抽屉**，默认进入查看态，点击“编辑”后复用同一抽屉进入编辑态，保存或取消后回到查看态。
- **导入会话**先产生**导入预览行**，再把通过校验且未被**重复候选**拦截的行写入账本；完成后以**导入结果摘要**说明新增、跳过、冲突和失败。
- **实际删除**会在确认影响后按使用者选择处理当前关联：要么删除当前收支详情中的全部流水，要么只删除当前流水并解散关联、让其他流水成为独立条目；不提供用户可见的版本或恢复入口，同一业务行再次导入时可以重新发布。
- **校准覆盖**只保存用户改过的字段和最近来源值；未被覆盖的字段在再次导入时采用最新来源值。
- **取消关联**把当前关系转为**关联忽略决定**；自动扫描不得再次建立同一组流水的关系，使用者主动添加时可以重新确认。
- 关系重建只读取现金流水的当前字段并重建派生结果，不能反向修改现金流水；再次导入是否覆盖字段只由**校准覆盖**决定。
- **关系种类不变量**在关系确认和投影构建前保护真实收支；**来源摘要**在全量发布前确认构建输入未被并发修改。
- **关系图互斥约束**以完整的已确认关系连通组判断，不因两条关系没有共享同一端点而失效；冲突候选保留为待审核关系，不阻断原始账单导入。
- **历史关系修复**只修改经授权的关系状态并记录原因；完成后重新生成**收支投影**，不能以删除原始账单或关系证据代替修复。
- **收支账本**与**投资账本视图**展示同一工作区的不同派生结果；020 不把投资事件纳入收支投影。
- **行情报价**先生成**计价币种市值**；指定**展示币种**且汇率可用时，再生成**折算市值**，结果
  是否可用由**估值状态**说明。
- **当前浮盈亏**使用当前市值减总成本，**浮盈亏率**的分母使用总成本绝对值，因此负成本不会把收益方向反转；合并同一标的时先汇总数量和成本，再重新计算**均摊成本**与浮盈亏。
- 单个标的的**仓位**使用该标的当前市值除以全部投资标的与现金的当前市值，因此现金会降低各投资标的的仓位比例。
- **表现周期**的总**周期盈亏**使用期末资产总额减期初资产总额，再扣除**外部出入金影响**；买卖是投资标的与现金之间的内部资产变化，不作为外部资金流扣除。
- **持仓展示偏好**只影响浏览器里的分组、排序、筛选、币种和周期显示，不改变账本记录、持仓事实或服务端计算结果。
- **连接器**从**数据源**增量获取数据，并通过**同步游标**记录已处理的位置；`source_type` 表示
  数据进入账本时的**导入渠道**，不等同于连接器配置中的 `provider` 名称。
- 工行账单的**来源摘要**独立于**对手方**；只有来源摘要精确为 `退货`，并生成匹配导入渠道的**正式退款信号**，
  才能进入工行退款关系候选。`退款` 或对手方文本中的关键词不能替代该信号。
- **支付账单导出**先按来源加载完整的**渠道流程**，再选择独立的**控制后端**。渠道流程负责应用内业务操作和状态判断；控制后端只负责界面观察、输入、点击、滚动、认证交接、连接恢复和通用文件保存。
  新增支付渠道时增加渠道流程；新增 Android + ADB 等控制能力时增加控制后端，两者不得相互复制职责。
- **认证接管**完成前不得继续提交或下载；申请已提交但**证明生成状态**未确认时不得重复申请，下载完成也不代表文件已经保存或转交完成。
- **收支分类**与**流水类型**相互独立：前者由使用者在**分类目录**中维护，后者表达现金流水自身的经济事实语义；分类不得参与关系匹配或财务计算。
- 一笔收支详情的全部现金流水共享同一**收支分类**。收支投影从**展示基准流水**读取分类；使用者修改分类时同步全部成员，建立关联时以展示基准流水为准静默统一。
- **分类目录**属于一个**工作区**；一个分类可以有多个子分类，但层级不得超过 5 层。选择父分类筛选时包含全部后代，父分类本身也可以直接分配给收支详情。
- 删除已使用的叶子分类时，直接使用该分类的收支详情全部变为**无分类**；有子分类的节点不得删除。

## 示例对话

> **开发者：** 一个现金账户里同时有 CNY 和 USD，文档里叫两个“币种口袋”吗？
>
> **领域人员：** 叫两项**分币种余额**；`pocket` 只保留在确实存在该内部概念的代码语境中。
>
> **开发者：** 导入后怎样说明一条记录为什么没有重复入账？
>
> **领域人员：** **导入渠道**和**业务行标识**共同确定幂等身份，**来源行快照**只用于溯源，
> 不是另一份待入账的原始记录。
>
> **开发者：** CLI 提示 `open-leg accept requires --other` 应该怎么写？
>
> **领域人员：** 写成“确认**待配对关系**时，必须通过 `--other` 指定**对侧流水**”。
>
> **开发者：** `market_value` 应显示为“本币市值”吗？
>
> **领域人员：** 不建议。“本币”容易被理解成用户所在国家的币种，这里应写**计价币种市值**；
> 换算到用户选择的币种后才叫**折算市值**。
>
> **开发者：** `swap` 的 `from_ticker` 和 `to_ticker` 写成支出腿和收入腿吗？
>
> **领域人员：** 不写“腿”，分别解释为**付出资产**和**换入资产**。

> **开发者：** 两个账户都持有同一标的，合并后怎么计算？
>
> **领域人员：** 数量相加、总成本相加，再用总成本除以总数量得到**均摊成本**；如果成本币种不同，就保持分开展示。

> **开发者：** 最近 24 小时买入了一部分标的，资产增加是不是收益？
>
> **领域人员：** 不是。**周期盈亏**比较期初和期末资产，并扣除**外部出入金影响**；买卖保留在资产变化里，不把本金当收益。

> **开发者：** 两条现金流水已经确认是同一笔支付，收支账本还显示两行吗？
>
> **领域人员：** 不显示。它们成为同一个收支详情，展示字段由内部的**展示基准流水**提供；页面用
> **关联流水**说明另一条记录对结果的影响，不要求用户理解主次。

> **开发者：** 一笔 100 元消费后来退款 30 元，退款当天会多一条收入吗？
>
> **领域人员：** 不会。退款并入原消费的**收支投影**，条目仍归属原消费时间，金额冲销为 70 元；
> 退款作为**关联流水**说明冲销影响。

> **开发者：** 微信转账被退回，和一般“撤销交易”应该使用同一类型吗？
>
> **领域人员：** 不使用同一类型。微信或支付宝的转账、红包和群收款退回是**转账退回记录类型**；银行的撤销、冲正才是**撤销记录类型**。只有来源明确表达消费退货或退款的**消费退款记录类型**才进入退款关系；提现还要按方向区分**提现出账记录类型**和**提现入账记录类型**。

> **开发者：** “个人转账”要放进**流水类型**下拉框吗？
>
> **领域人员：** 不放。单条流水选择转账入账或转账出账，使用者再为一条负金额流水和一条正金额流水选择**个人转账关系**；金额只决定**流水方向**。

> **开发者：** 用户取消关联后，下次扫描还能再次推荐吗？
>
> **领域人员：** 不能。取消后保留一条当前的**关联忽略决定**；这不是关系历史，只有使用者主动重新添加时才恢复关联。

> **开发者：** 账单只给出掩码账号时，关系匹配可以再按字符串长度猜它是尾号吗？
>
> **领域人员：** 不可以。导入时必须同时生成**对方账号**和**对方账号属性**；关系匹配只消费这两个正式字段，属性缺失或矛盾时不使用账号证据。

> **开发者：** 收支账本里的每一行都能直接点“编辑”吗？
>
> **领域人员：** 可以编辑实际业务字段。**来源保护**只限制**导入渠道**、**业务行标识**和**来源行快照**；用户修改当前值时形成**校准覆盖**，不创建修正版流水。
>
> **开发者：** 导入文件选完就直接写入可以吗？
>
> **领域人员：** 不可以。先进入**导入会话**，展示**导入预览行**和**重复候选**，用户确认后才写入；结束时提供**导入结果摘要**。

> **开发者：** 文件里识别出三个来源账户，已有别名都能匹配时，可以直接进入流水预览吗？
>
> **领域人员：** 不可以。系统可以用既有**账单账户映射**和唯一**账户别名**生成预选，但使用者仍要逐组完成**账户映射确认**。只有最终确认导入成功，决定才保存为新的**账单账户映射**。

> **开发者：** 现有账户还没配置本次账单里的 USD，选中它后要马上改账户配置吗？
>
> **领域人员：** 不马上改。把 USD 记为**账户币种扩充草稿**，在对应映射行的账户选择下方用小字说明；最终确认导入时和流水一起提交。

> **开发者：** 用户删除一条导入流水后，系统要不要一直保留它的历史版本？
>
> **领域人员：** 不需要。执行**实际删除**，让它从当前账本消失；以后再次导入同一**业务行标识**时，可以重新发布。

> **开发者：** “餐饮”应该写进**流水类型**还是**收支分类**？
>
> **领域人员：** 写进**收支分类**。**流水类型**表达消费、退款或转账等经济事实；“餐饮”属于使用者维护的整理标签。
>
> **开发者：** 两条流水建立同笔支付关系前分类不同，应该保留两个值吗？
>
> **领域人员：** 不保留。建立关联时以**展示基准流水**为准，同步整笔收支详情的分类；以后解除关联，各流水保留同步后的分类。

## 已废弃的模型术语

以下术语只用于阅读历史 Spec 或迁移说明，不得重新作为当前领域模型写入新文档或程序文案。

| 历史术语 | 当前表达或处理方式 |
|----------|--------------------|
| `RawRecord` / 原始记录 | 已移除独立实体；当前账本记录通过**来源行快照**保存行级溯源。 |
| `ImportBatch` / 导入批次 | 已移除持久化实体；不要用它解释当前幂等或审计边界。 |
| `Source Artifact` / 来源文件档案 | 已移除持久化实体；不要把来源行快照描述成文件档案。 |
| 持久化 `Revision` / 记录修订 | 当前不保存改账版本链；不要暗示存在可查询的记录修订实体。 |
| `Relation Check Run` / 关系检查运行 | 已移除持久化实体；当前关系审查不依赖该历史运行记录。 |
| 独立删除事件 | 当前变更不新增操作日志实体；实际删除的持久化细节不得在用户文案中描述成版本或恢复记录。 |

## 待澄清歧义

- 英文 `leg` 没有统一的中文逐词译法。必须先判断它表示金额组成、资产方向还是关系端点，再使用
  **现金部分**、**证券部分**、**付出资产**、**换入资产**或具体的流水名称。
- `fact` 在内部模型中表示已进入账本的正式记录；面向普通用户时使用**账本记录**、**现金流水**或
  **投资事件**，仅在数据模型和审计语境中保留 `Formal Fact` 或“事实”。
- 账户类型 `loan` 和 `lend` 目前分别显示为“贷款”和“借款”，但双方资金方向和债权债务口径不够
  明确，可能产生反向理解；在业务规则确认前，不新增统一中文名称，也不交换现有枚举值。
- “本币”在现有估值材料中有时指每个标的的行情计价币种，并不一定是用户的本国币种或系统基准
  币种；新文案应根据含义使用**计价币种**、**展示币种**或直接写具体币种。
- “投影”可以用于描述从账本记录派生视图的内部架构，但面向用户时应改为“计算结果”“报表”或
  具体业务名称。020 的领域文档使用**收支投影**和**投影条目**，页面标题仍使用**收支账本**，
  不直接向普通用户显示 `projection`。
- `provider` 可能指行情服务方、同步服务方或连接器配置键；中文文案应按上下文写**数据源**或具体
  服务方名称，不能一律保留为 `provider`。
- “菱形路径”是关系图匹配的内部技术说法，不是稳定的业务概念；除算法设计文档外，应直接说明
  涉及的流水和关系。
- “分类”在历史代码和文档中曾同时表示旧 `category` 业务值、`record_type` 和用户标签。新文档必须用
  **流水类型**表达经济事实语义，用**收支分类**表达使用者维护的树状标签；面向用户的局部控件可简写为“分类”。
