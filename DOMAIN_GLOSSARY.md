# 领域统一术语

本词表记录项目中需要统一使用的业务语言。代码标识符、数据库字段、CLI 参数、枚举值和协议值保持
原样；文档、注释和程序文案应根据目标读者使用下列规范术语。词条后的 `(new)` 表示本轮扫描新增。

## 账本与账户

| 规范术语 | 定义 | 应避免的别名 |
|----------|------|--------------|
| **工作区** (new) | 隔离一组账户、账本记录、交易关系和估值数据的业务边界，对应内部标识 `workspace`。 | 租户（未讨论租户模型时）、账套空间 |
| **账户** (new) | 用来分别记录现金、负债、债权或投资持仓的账本对象，账户名称在工作区内唯一。 | 钱包（非产品名称时）、`account`（中文文案中） |
| **分币种余额** (new) | 同一账户按币种分别记录的余额集合。 | 币种口袋、余额口袋、`balance pocket`、`currency pocket` |
| **账本记录** (new) | 已进入账本并可参与查询、审计或计算的业务记录，是面向用户描述内部 `Formal Fact` 的总称。 | 正式事实、`fact`（面向普通用户时） |
| **现金流水** (new) | 记录某个账户中一笔现金增减及其时间、币种、分类和来源的账本记录。 | 现金事实、现金 `fact`、现金交易行（面向用户时） |
| **投资事件** (new) | 记录证券或加密资产买卖、换入换出、出入金、分红或校准的账本记录。 | 投资事实、`investment fact`（中文文案中） |
| **余额校准** (new) | 在指定时点把账户某一币种的余额确认为已知正确值。 | 余额核对、余额校正、余额 `checkin`（中文文案中） |
| **持仓校准** (new) | 在指定时点把某一标的的持有数量和成本信息确认为已知正确值。 | 持仓核对、持仓校正、持仓 `checkin`（中文文案中） |

## 导入与溯源

| 规范术语 | 定义 | 应避免的别名 |
|----------|------|--------------|
| **导入渠道** (new) | 标识账本记录由哪一种账单、券商文件或解析流程产生，对应 `source_type`。 | 来源类型、渠道 `kind`、`source type`（中文文案中） |
| **业务行标识** (new) | 数据源为一条业务记录提供或导入器根据内容生成的稳定标识，对应 `record_id`。 | 业务行身份、行 `identity`、裸 `record id`（中文文案中） |
| **来源行快照** (new) | 导入时随账本记录保存的原始行级内容，用于匹配、审计和排障，对应 `source_payload`。 | 原始 `payload`、行内溯源、原始记录（容易误指已删除的 `RawRecord`） |
| **扩展业务信息** (new) | 投资事件中未提升为正式字段、但仍具有业务意义的补充信息，对应 residual `payload`。 | `residual`、剩余 `payload`、来源快照 |

## 投资交易

| 规范术语 | 定义 | 应避免的别名 |
|----------|------|--------------|
| **现金部分** | 一笔投资事件中表示现金金额的组成部分；手续费是否包含在内由对应数据源的费用合同决定。 | 现金腿、`cash leg`（面向用户的中文说明中） |
| **证券部分** | 一笔投资事件中表示证券标的及其数量的组成部分。 | 证券腿、`security leg`（面向用户的中文说明中） |
| **付出资产** | `swap` 中被交付或扣减的资产，对应 `from_ticker` 和 `from_amount`。 | 支出腿、from 腿 |
| **换入资产** | `swap` 中收到或增加的资产，对应 `to_ticker` 和 `to_amount`。 | 收入腿、to 腿 |

## 关系匹配

| 规范术语 | 定义 | 应避免的别名 |
|----------|------|--------------|
| **关系审查列表** (new) | 集中展示待人工判断、确认或驳回的交易关系候选项。 | 审查收件箱、`Review Inbox`（中文文案中） |
| **同笔支付关系** (new) | 表示两个渠道中的两条现金流水实际记录了同一笔支付，对应 `payment_mirror`。 | 支付镜像、镜像支付、`payment mirror`（中文文案中） |
| **转账配对关系** (new) | 表示两条现金流水分别是同一笔内部转账的转出和转入，对应 `transfer_pair`。 | 转账腿、`transfer pair`（中文文案中） |
| **退款冲销关系** (new) | 表示一笔退款全部或部分抵销原支出的关系，对应 `refund_offset`。 | 退款腿、退款 `offset`（中文文案中） |
| **信用账户还款关系** (new) | 表示现金账户付款与信用账户负债减少属于同一次还款的关系，对应 `credit_repayment`。 | 信用还款 `subtype`、还款腿 |
| **待配对关系** | 已确定一笔锚点流水，但尚未唯一确定另一笔流水的待审核关系；内部状态称为 `open-leg pending`。 | 开放单腿、单腿 `pending`、`open-leg`（面向用户的中文说明中） |
| **锚点流水** | 发起关系匹配并在待配对期间保持确定的那笔流水，对应内部的 anchor fact。 | 锚点腿、anchor 腿 |
| **对侧流水** | 与锚点流水共同组成完整关系的另一笔流水，对应 `other_fact_id` 或 secondary fact。 | 对侧腿、另一腿 |

## 持仓与估值

| 规范术语 | 定义 | 应避免的别名 |
|----------|------|--------------|
| **行情报价** (new) | 某一数据源在指定时点给出的标的价格及其计价币种。 | `quote`（中文文案中）、行情价格观测值（面向用户时） |
| **持仓估值** (new) | 根据持有数量和可用行情报价计算出的持仓价值。 | 持仓投影、`portfolio valuation`（中文文案中） |
| **计价币种** (new) | 行情报价用来表示标的价格的币种。 | 本币、当地币种、`local currency`（中文文案中） |
| **计价币种市值** (new) | 按标的行情报价所用币种表示的持仓市值。 | 本币市值、`local market value`（中文文案中） |
| **展示币种** (new) | 用户指定用于汇总或显示估值结果的目标币种，对应 `display_currency`。 | 基准币种（未定义基准时）、`display currency`（中文文案中） |
| **折算市值** (new) | 将计价币种市值按可用汇率换算为展示币种后的金额。 | 展示市值、转换后市值、`converted value`（中文文案中） |
| **估值状态** (new) | 表明一次估值是否完整、是否过期或是否因缺少行情或汇率而受限的结果状态。 | 价格状态、`status`（未说明含义时） |

## 数据同步

| 规范术语 | 定义 | 应避免的别名 |
|----------|------|--------------|
| **数据源** (new) | 提供账本记录、行情报价或汇率数据的外部系统或服务。 | `provider`（中文文案中）、供应商（未讨论商业主体时） |
| **连接器** (new) | 按项目约定从一种数据源读取、转换并提交数据的适配组件。 | 接头、`connector`（中文文案中） |
| **同步游标** (new) | 连接器记录的增量同步位置，用于确定下一次同步从哪里继续。 | 同步指针、`cursor`（中文文案中） |

## 概念关系

- 一个**工作区**包含多个**账户**；一个账户可以有多项**分币种余额**。
- **现金流水**和**投资事件**都是**账本记录**，但分别表达现金变动和投资资产变动。
- 一条导入产生的账本记录由**导入渠道**和**业务行标识**共同确定幂等身份，并可携带一份
  **来源行快照**；投资事件还可携带**扩展业务信息**，两者用途不同。
- 一笔 `swap` 同时包含**付出资产**和**换入资产**；其中一方可以是**现金部分**，另一方可以是
  **证券部分**。
- 一条**待配对关系**必须有一笔**锚点流水**，但在完成配对前可以没有**对侧流水**；确认后才成为
  完整的**转账配对关系**或**退款冲销关系**。
- **行情报价**先生成**计价币种市值**；指定**展示币种**且汇率可用时，再生成**折算市值**，结果
  是否可用由**估值状态**说明。
- **连接器**从**数据源**增量获取数据，并通过**同步游标**记录已处理的位置；`source_type` 表示
  数据进入账本时的**导入渠道**，不等同于连接器配置中的 `provider` 名称。

## 示例对话

> **开发者：** 一个现金账户里同时有 CNY 和 USD，文档里叫两个“币种口袋”吗？
>
> **领域人员：** 叫两项**分币种余额**；`pocket` 只保留在确实存在该内部概念的代码语境中。
>
> **开发者：** 导入后怎样说明一条记录为什么没有重复入账？
>
> **领域人员：** **导入渠道**和**业务行标识**共同确定幂等身份，**来源行快照**只用于溯源，
> 不是另一份待入账的原始记录。
>
> **开发者：** CLI 提示 `open-leg accept requires --other` 应该怎么写？
>
> **领域人员：** 写成“确认**待配对关系**时，必须通过 `--other` 指定**对侧流水**”。
>
> **开发者：** `market_value` 应显示为“本币市值”吗？
>
> **领域人员：** 不建议。“本币”容易被理解成用户所在国家的币种，这里应写**计价币种市值**；
> 换算到用户选择的币种后才叫**折算市值**。
>
> **开发者：** `swap` 的 `from_ticker` 和 `to_ticker` 写成支出腿和收入腿吗？
>
> **领域人员：** 不写“腿”，分别解释为**付出资产**和**换入资产**。

## 已废弃的模型术语

以下术语只用于阅读历史 Spec 或迁移说明，不得重新作为当前领域模型写入新文档或程序文案。

| 历史术语 | 当前表达或处理方式 |
|----------|--------------------|
| `RawRecord` / 原始记录 | 已移除独立实体；当前账本记录通过**来源行快照**保存行级溯源。 |
| `ImportBatch` / 导入批次 | 已移除持久化实体；不要用它解释当前幂等或审计边界。 |
| `Source Artifact` / 来源文件档案 | 已移除持久化实体；不要把来源行快照描述成文件档案。 |
| 持久化 `Revision` / 记录修订 | 当前不保存改账版本链；不要暗示存在可查询的记录修订实体。 |
| `Relation Check Run` / 关系检查运行 | 已移除持久化实体；当前关系审查不依赖该历史运行记录。 |
| 独立删除事件 | 当前现金流水使用逻辑删除字段；不要描述成单独的删除事件实体。 |

## 待澄清歧义

- 英文 `leg` 没有统一的中文逐词译法。必须先判断它表示金额组成、资产方向还是关系端点，再使用
  **现金部分**、**证券部分**、**付出资产**、**换入资产**或具体的流水名称。
- `fact` 在内部模型中表示已进入账本的正式记录；面向普通用户时使用**账本记录**、**现金流水**或
  **投资事件**，仅在数据模型和审计语境中保留 `Formal Fact` 或“事实”。
- 账户类型 `loan` 和 `lend` 目前分别显示为“贷款”和“借款”，但双方资金方向和债权债务口径不够
  明确，可能产生反向理解；在业务规则确认前，不新增统一中文名称，也不交换现有枚举值。
- “本币”在现有估值材料中有时指每个标的的行情计价币种，并不一定是用户的本国币种或系统基准
  币种；新文案应根据含义使用**计价币种**、**展示币种**或直接写具体币种。
- “投影”可以用于描述从账本记录派生视图的内部架构，但面向用户时应改为“计算结果”“报表”或
  具体业务名称，不应在没有解释的情况下直接显示 `projection` 或“投影”。
- `provider` 可能指行情服务方、同步服务方或连接器配置键；中文文案应按上下文写**数据源**或具体
  服务方名称，不能一律保留为 `provider`。
- “菱形路径”是关系图匹配的内部技术说法，不是稳定的业务概念；除算法设计文档外，应直接说明
  涉及的流水和关系。
