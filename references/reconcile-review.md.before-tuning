# 合并审查（步骤 ⑤）— 分批 subagent 审查模式

`ft reconcile` 后，审计文件生成在 `~/.ft/audit/reconcile/`。三年数据审计文件约 2,888 行（约 1,444 对 保留/去除），需要用 **subagent 分批并行审查**。

## 分片策略

按 3 个月一个批次（12 批），每批约 200-400 对去重记录。并行 3 个 subagent，4 轮审完。

```python
# 分片逻辑示例
audit_lines = [line for line in open(audit_csv) if line.strip()]
# 解析日期，按 3 个月分段
```

## 每个 subagent 接收的内容

```python
context = f"""
审计文件：{audit_csv}
记录目录：{records_cash_dir}
贷款目录：{records_loan_dir}

请审查 {start_date} 至 {end_date} 的数据。
"""
```

## 审查重点

### 1. 去重正确性

- 审计文件中每对 `dedup_status=保留` / `dedup_status=去除` 应该对应**同一天的同笔交易**，跨来源（微信 vs 工行、支付宝 vs 工行等）
- 保留行应在 records CSV 中存在
- 去除行应该已被标记（不应再出现在有效记录中）
- **误删检查**：金额/counterparty 不匹配却被配对的
- **漏删检查**：同一天同金额同 counterparty 跨来源的明显重复未被去重的

### 2. 个人转账识别

扫描 records 中 counterparty 为个人姓名的交易：

| 姓名 | 关系 | 特征 |
|------|------|------|
| 黄文龙 | 本人 | 基金买卖、购汇、信用卡还款、手机银行转账 |
| 梁碧玲 | 家人 | 每月固定 -2,000（规律性支出） |
| 黄雨生 | 家人 | 每月固定 -1,000（规律性支出） |
| 金哲玄 | 朋友/同事 | 季度性 +3,500~4,000 汇入 |

注意区分：
- **本人账户间转账**（基金、购汇、还款）→ 当前被标为 expense/income，是银行视角的正确分类
- **家庭规律性转账**（梁碧玲/黄雨生月付）→ 当前为 expense（从付款方角度正确）
- **他人汇入**（金哲玄）→ 当前为 income（从收款方角度正确）

## 输出格式

```markdown
### 去重正确性
- 共 X 对，全部/部分匹配
- 误删：X 处（明细）
- 漏删：X 处（明细）

### 个人转账
| 日期 | 金额 | 对方 | 方向 | 说明 |
```

## 常见问题

- **去除行仍存在于 records 文件中**：这是正常现象。审计文件仅标记 `dedup_status` 字段，**不实际删除记录行**。物理删除需要额外脚本。
- **审计文件只覆盖跨来源重复**（微信/支付宝 vs 银行卡）。单来源、个人转账等通常不在审计文件中。
- **时间戳 ±1 秒不匹配**：不同来源的时间戳可能有 1 秒差异，精确字符串匹配会失败，但审计系统的时间窗口匹配能正确配对。
