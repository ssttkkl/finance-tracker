flowchart TD
    A[读取原始账单] --> B[解析为 raw records]
    B --> C[识别 expense / refund / other]
    C --> D[逐条处理 refund]

    D --> E[先收集订单级候选]
    E --> F{找到候选?}

    F -- 是 --> G[候选排序]
    F -- 否 --> H[尝试渠道专用候选]
    H --> I{找到候选?}

    I -- 是 --> G
    I -- 否 --> J[尝试 cp 匹配]
    J --> K{找到候选?}

    K -- 是 --> G
    K -- 否 --> L[尝试 description fallback]
    L --> M{找到候选?}

    M -- 是 --> G
    M -- 否 --> N[尝试 gross candidate]
    N --> O{找到候选?}

    O -- 是 --> G
    O -- 否 --> P[作为孤退款保留为 income]

    G --> Q[选出最佳候选]
    Q --> R[判定 strong / weak]
    R --> S{全额退款?}

    S -- 是 --> T[核销整笔原消费]
    S -- 否 --> U[冲减原消费净额]

    T --> V[写入 tracking_pairs]
    U --> V
    P --> V

    V --> W[输出 records + tracking_pairs]
