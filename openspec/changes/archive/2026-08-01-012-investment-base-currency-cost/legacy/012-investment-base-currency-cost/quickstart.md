# Quickstart

```bash
uv run pytest tests/unit/domain/test_investment_base_currency_cost.py -q
# account with bases:
# ft acct add 盈立 --type security --currency USD
# ensure metadata base_currencies includes USD HKD (seed from --currency + manual if needed)
```
