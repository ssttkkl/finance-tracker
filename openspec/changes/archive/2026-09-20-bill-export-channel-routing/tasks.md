## 1. 思考

- [x] 1.1 阅读仓库工作流、项目上下文、原支付宝 Skill、PR 变更和已验证的微信导出流程。
- [x] 1.2 确认目标：统一入口按渠道路由；渠道文件承载完整业务流程；控制后端只承载界面控制技术。

## 2. 计划

- [x] 2.1 维持 B 类纯技能/文档变更和 `skip_specs: true`，不修改产品主规格或运行时代码。
- [x] 2.2 定义统一入口、渠道流程、控制后端的职责和扩展规则。

## 3. 任务拆分与一致性

- [x] 3.1 将目录迁移、渠道路由、两个渠道流程、通用后端、元数据和文档拆分为独立任务。
- [x] 3.2 检查 proposal、design、tasks 对流程/后端边界的表述一致。

## 4. 构建

- [x] 4.1 将 Skill 迁移为 `.agents/skills/bill-export/`。
- [x] 4.2 编写统一路由和通用状态契约。
- [x] 4.3 编写微信支付与支付宝完整渠道流程。
- [x] 4.4 编写只包含 Computer Use + iPhone 镜像控制能力的通用后端。
- [x] 4.5 更新技能元数据、README 和领域词表。

## 5. 审查

- [x] 5.1 范围复核：未修改应用代码、数据库、接口或 UI。
- [x] 5.2 边界复核：渠道文件包含业务路径；后端文件不包含微信或支付宝路径。
- [x] 5.3 安全复核：认证由用户接管；未授权不转交；各渠道状态和文件证据隔离。

## 6. 测试与 QA

- [x] 6.1 运行 Skill frontmatter 校验和 YAML 解析：通过，入口为 `$bill-export`。
- [x] 6.2 运行 OpenSpec 严格校验和 doctor：19 项通过，0 项失败；doctor 通过。
- [x] 6.3 运行 `git diff --check`，检查旧名称、失效链接和职责残留：通过；旧名称只保留在迁移与回滚说明中，控制后端没有渠道业务路径。
- [x] 6.4 记录验证 HEAD、比较基线、命令、结果和残余风险。

### 验证证据

- 执行时间：`2026-08-12 17:47:44 +0800`。
- 修改前 HEAD：`8876bb3f902aff519b94176e81f2ffbbfa45704f`；比较基线 `origin/refactor/web`：`04caf0c9c412e1cc72963290a1b34968965d2515`。
- `quick_validate.py .agents/skills/bill-export`：`Skill is valid!`。
- `agents/openai.yaml` 解析：通过，默认提示词使用 `$bill-export`。
- `openspec validate --all --strict`：19 项通过，0 项失败；`openspec doctor`：通过。
- `git diff --check`：通过。
- 未运行 Python、Web、数据库或 UI 测试：本次只修改 Skill 与文档，不包含可执行代码、持久化、接口、路由或界面实现；残余风险限于支付应用页面后续变化。

## 7. 发布

- [x] 7.1 按用户授权提交并推送到 PR #43 的现有分支，不创建新 PR、不合并或部署。

## 8. 反思

- [x] 8.1 将“渠道流程描述业务操作、控制后端描述工具能力”的边界写入技能、词表和设计记录。
