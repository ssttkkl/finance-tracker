## 1. Reproduce and lock the correctness failure

- [x] 1.1 Record the real-bill reproduction and confirm the active baseline is `origin/refactor/web` at `805e512`. Real evidence is in `/Users/huangwenlong/.ft/qa-results/real-bills-browser-qa-20260816.md`; `grill-me /grilling` was unavailable in this runtime and the confirmed scope is recorded in the change artifacts.
- [x] 1.2 Add and run the failing domain regression for an accepted `payment_mirror` component whose refund must not cross-match a same-amount different-merchant expense.
- [x] 1.3 Add regressions for event-level merchant evidence, ambiguous evidence staying `pending_review`, and an already-consumed mirrored refund not generating a duplicate offset.

## 2. Repair event-level refund planning

- [x] 2.1 Separate refund-leg occupancy from `payment_mirror` participation while retaining mirror expansion for already-consumed refund legs.
- [x] 2.2 Preserve accepted mirror components as refund candidate event groups with aggregated merchant, title/order, source and structured refund evidence.
- [x] 2.3 Rank refund candidates by event-level evidence and fail closed when the highest-evidence event is not unique; keep exact Decimal and partial-refund semantics.
- [x] 2.4 Run the affected domain and application relation tests after each implementation slice and mark the corresponding regressions green: 97 passed, 3 skipped.
- [x] 2.5 After reverse-order QA exposed Phase A hard-key refund overdraw, add the event-level amount gate: once Phase B reveals a mirror component, retain a duplicate hard-key proposal as evidence but demote it to `pending_review` when accepted refunds already consume the event amount. Regression: `tests/test_import_relation_planning.py::test_alipay_hard_refund_is_pending_when_mirror_event_already_refunded`.

## 3. Integration and backend parity

- [x] 3.1 Add an import/relation integration regression covering the overlapping Alipay/WeChat/CCB ordering and verify no cross-merchant accepted `refund_offset` is persisted on SQLite.
- [x] 3.2 Add reverse-order, single-source, rescan/idempotency, accepted-refund-mirror duplicate, and mirror-event partial-remaining regressions. Focused result on SQLite: `47 passed, 6 skipped` (`tests/test_transaction_relations_cross_batch.py` + `tests/test_transaction_relations_refund.py`).
- [x] 3.3 Run the SQLite relation/import contract matrix and the same matrix against an explicitly configured `_test` PostgreSQL database. On 2026-09-04, SQLite focused matrix passed `85 passed, 16 skipped`; with `FT_TEST_POSTGRES_URL=postgresql+psycopg://postgres:.../finance_tracker_refund_test` and `FT_REQUIRE_TEST_POSTGRES=1`, the same matrix passed `101 passed` with no skips. The PostgreSQL service was an isolated `postgres:16-alpine` container named `finance-tracker-refund-postgres-test`, database `finance_tracker_refund_test`; it was stopped after verification.
- [x] 3.4 Run the full Python test suite, strict OpenSpec validation, `openspec doctor`, `git diff --check`, and a focused final diff review. The final full run with the explicit PostgreSQL test URL completed `1725 passed, 10 skipped, 9 failed, 1 warning` in 898.19 seconds. The 9 failures are outside this change: 6 PostgreSQL projection-concurrency assertions, account valuation fallback, PostgreSQL dataset-index downgrade blocked by the documented one-shot cash-category migration, and the SQLite 100k-fact wealth cold-p95 budget (`5.084s` vs `5s`). No relation/import test failed. `openspec validate --all --strict` passed `31/31`; `openspec doctor` reported the root and references valid; `uv run python -m compileall -q src tests/test_import_relation_planning.py tests/test_transaction_relations_cross_batch.py`, `uv build`, and `git diff --check` passed. Final diff review covered the planner, pipeline, refund matcher, relation regressions, and this change's artifacts; no blocking finding remained.

## 4. Real browser QA and delivery readiness

- [x] 4.1 Run the production-preview browser flow with isolated workspace/database for the overlapping real bills in forward and reverse order. Production frontend preview: `http://127.0.0.1:5181`; API preview: `http://127.0.0.1:8011`; frontend was built with `VITE_FT_API_ORIGIN=http://127.0.0.1:8011 npm run build` and served with `npm run start -- --port 5181`. Forward workspace URL: `http://127.0.0.1:5181/w/8901c92c-9654-428a-bc66-e8f34167f780/`, SQLite database `/tmp/ft-repair-browser.hYmRor/forward-fixed-20260904.db`; reverse workspace URL: `http://127.0.0.1:5181/w/d7d61ded-64cf-437d-86bf-16c576425eac/`, SQLite database `/tmp/ft-repair-browser.hYmRor/reverse-fixed-20260904.db`. Both used Chromium at `1440x900`; the relation pages were additionally checked with 100 rows per page. Forward order was Alipay → WeChat → CCB; reverse order was CCB → Alipay → WeChat. All six scan/preview/commit sequences completed successfully.
- [x] 4.2 Verify normal preview/confirm succeeds through the cached-plan flow, no `import_relation_preview_stale` is exposed, and the highde refund is never accepted against the 玉渊潭公园 expense. Forward result: Alipay `1362`, WeChat `1170`, CCB `868` imported successfully; CCB relation preview showed `269` total, `162` automatic, `107` pending. Reverse result: CCB `868`, Alipay `1362`, WeChat `1170` imported successfully; after CCB-first, the Alipay relation preview showed `202` total, `108` automatic, `94` pending, and the 2024-04-21 highde refund row was pending rather than automatic. Both confirmation APIs returned HTTP 200, with no `import_relation_preview_stale`; browser console contained only the expected unauthenticated startup `GET /api/v1/auth/session` 401 responses. Forward screenshot: `/tmp/ft-repair-browser-forward-fixed-ccb-relations.png`; reverse CCB screenshot: `/tmp/ft-repair-browser-reverse-fixed-ccb-relations.png`; reverse Alipay target screenshot: `/tmp/ft-repair-browser-reverse-fixed-alipay-target.png`.
- [x] 4.3 Record browser URLs, viewports, screenshots, network/console results, database evidence, current `HEAD`, baseline, residual risks, and rollback notes in this change's `tasks.md`. Forward database evidence: accepted `refund_offset` `1191 → 1197` and `1150 → 2682`, accepted mirrors `1191 → 2668` and `2362 → 2677`; no accepted highde refund targeted the 玉渊潭 expense `2362` or `2677`. Reverse database evidence: existing accepted CCB `refund_offset` `136 → 150`, accepted mirror `2059 → 136`, and pending Alipay hard-key `refund_offset` `2059 → 2065`; no accepted refund relation targeted the 玉渊潭 expenses `145` or `3230`. Current `HEAD` and comparison baseline are both `805e512f65a15286f2a2750acb99e4cd00f237fb` (`origin/refactor/web`). Residual risks: 9 unrelated full-suite failures listed in 3.4 remain; pending duplicate proposals require human review; historical incorrect relations are not silently rewritten. Rollback is application-version rollback only; no schema migration was added, and staged plans can expire under the existing TTL.
- [x] 4.4 Do not commit, push, create a PR, or merge this second change without explicit authorization for this change's target branch. No commit, push, PR, merge, deployment, or external financial-data write was performed; the worktree remains available for review.
