## 1. 思考与计划

- [x] 1.1 复核未配对流水、账户别名、关系状态与现有 7 天规则，定位索引日期范围短于业务窗口。
- [x] 1.2 编写 proposal、delta 规格和设计，明确不重扫真实数据库。

## 2. 测试先行

- [x] 2.1 新增使用 `FactCandidateIndex` 的回归测试，覆盖跨三个自然日但不足 7 天的同币种跨境汇款，并确认修复前失败（`1 failed, 4 passed`）。

## 3. 实现

- [x] 3.1 由长窗口业务常量导出索引日期填充范围，使候选检索覆盖完整业务窗口。

## 4. 审查与验证

- [x] 4.1 完成范围化工程复核：候选仍按有限日期桶读取，最终精确秒数、严格 Decimal 金额、唯一别名及全局一对一分配保持不变；同步旧路径删除后的包边界断言。
- [x] 4.2 验证通过：关系回归 `24 passed`；本地 SQLite/PostgreSQL 契约 `10 passed`；完整回归（排除已批准跳过的 `test_wealth_performance.py`）`1160 passed, 122 skipped`；`uv build`、`compileall`、`git diff --check`、`openspec validate --all --strict` 均通过。

## 5. 发布与反思

- [x] 5.1 已记录：无需迁移；回滚恢复原候选范围且不删除事实或关系；要为既有流水补建关系，必须由用户明确授权后单独重扫真实库。
