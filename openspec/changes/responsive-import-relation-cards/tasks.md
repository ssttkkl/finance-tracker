## 1. 思考

- [x] 1.1 阅读 `openspec/project-context.md`、`cash-ledger-browser` 与相关导入规格、现有配对实现和测试，并以 `4adf682` 作为当前 `origin/refactor/web` 基线。
- [x] 1.2 通过 `/grilling` 明确范围：移动端单列关系卡片、保留卡片内决定控件、桌面表格保留、配对筛选对齐“核对流水”摘要卡片、配对阶段取消分页并按原始顺序连续展示。

## 2. 计划与原型

- [x] 2.1 完成 `proposal.md`、`specs/cash-ledger-browser/spec.md` 与 `design.md`，记录目标、非目标、响应式边界、关系决定语义、连续列表取舍和回滚策略。
- [x] 2.2 创建 `prototype/index.html`，覆盖桌面表格、移动单列卡片流、三张摘要筛选卡片、正常 / 加载 / 空 / 错误 / 成功、禁用控件和焦点状态。
- [x] 2.3 取得用户对卡片层级、摘要筛选卡片、连续展示方式和单列卡片流的确认；无未决 Open Questions。
- [x] 2.4 原型浏览器预检：本地静态 URL `http://127.0.0.1:4173/`，使用 Playwright Chromium 检查 `320`、`375`、`390`、`414`、`768`、`1440 px`；所有宽度无横向溢出，移动端显示卡片且隐藏表格，桌面端显示表格且隐藏卡片，未渲染分页器，筛选点击可切换可见关系，所有可见控件高度至少 `44 px`。截图：`/tmp/responsive-import-relation-cards-prototype/390.png`、`/tmp/responsive-import-relation-cards-prototype/1440.png`。控制台错误、页面异常和请求失败均为 0。

## 3. 任务拆分与一致性

- [x] 3.1 对照三项 delta requirement 复核任务覆盖关系卡片、摘要筛选卡片、连续列表、决定语义、响应式状态、键盘焦点和触控命中区。
- [x] 3.2 确认本变更不涉及 API、持久化、金额计算、关系规则、权限或数据库双后端契约；PostgreSQL 验证标记为不适用。

## 4. 构建

- [x] 4.1 在 `web/tests/CashImportPage.test.tsx` 先增加失败回归测试：配对结果超过原每页数量时不再出现分页器，且筛选结果连续展示并保持原始顺序；先确认旧实现因缺少摘要卡片而失败，再转绿。
- [x] 4.2 增加失败回归测试：配对筛选渲染为与“核对流水”同款的 `全部`、`自动`、`待处理` 摘要卡片，点击后更新可见关系和选中态。
- [x] 4.3 增加失败回归测试：移动视口对应的卡片结构保留自动、待处理、已配对、已拒绝、候选选择、关系类型和拒绝 / 撤销操作，且操作仍写入现有 `relationDrafts` / `relationDecision` 语义；桌面 / 移动真实浏览器覆盖见 6.4。
- [x] 4.4 基于共享关系数据和现有事件处理器实现桌面连续表格与移动单列卡片流；移除 `page`、`pageSize`、分页切片和分页器，不复制关系决定状态机。
- [x] 4.5 将配对筛选替换为复用“核对流水”视觉 token 的摘要卡片，保留数量、选中态、无障碍名称和键盘焦点；删除每页选择器。
- [x] 4.6 增加 `max-width: 820px` 的移动布局：隐藏桌面关系表格，显示单列卡片；桌面保留表格列顺序和全量筛选结果，长文本、候选控件、拒绝 / 撤销按钮不得造成横向溢出。
- [x] 4.7 保持正常、加载、空、错误和确认完成状态，以及自动关系只读、手动候选选择、拒绝 / 撤销和确认请求行为不变。

## 5. 审查

- [x] 5.1 完成产品 / 范围复核：移动端为单列连续卡片流，桌面保留全量表格，筛选对齐“核对流水”摘要卡片；无分页、无后端无限加载协议，符合已确认范围，无 finding。
- [x] 5.2 完成工程复核：关系筛选始终从共享 `relationItems` 和现有 `relationDrafts` 派生，决定仍由 `relationDecision` 提交；长列表不新增请求或数据协议，渲染性能风险已在 `design.md` 记录为后续虚拟列表议题；无阻断 finding。
- [x] 5.3 完成设计复核及 Hallmark `audit`：按 Hallmark 规则人工审查生产截图和 DOM；运行时没有可执行的 `audit` action，故未声称工具已执行。人工检查摘要卡片层级、状态、触控尺寸、键盘焦点及 320 / 390 / 1440 px，未发现 critical / major finding。
- [x] 5.4 完成最终范围化 diff 复核：实现、测试、规格和设计记录一致；生产配对代码无分页残留，未触及 API、数据库、金额、权限或新增依赖。另修正 `refactor/web` 已有导入导航导致的暗色侧栏测试 7→8 条断言漂移。

## 6. 测试与 QA

- [x] 6.1 2026-09-04，比较基线 `4adf6824b2acbcf6ffc641e7da7f8068605d143a`，执行 `openspec --version && openspec validate --all --strict && openspec doctor`：OpenSpec `1.7.0`，35 项校验通过，Doctor 根目录与引用检查通过。
- [x] 6.2 2026-09-04，执行目标测试 `npm test -- --run tests/CashImportPage.test.tsx -t "连续展示关系、使用摘要筛选卡片"` 及完整 `npm test`：13 个文件、138 个测试通过；覆盖筛选切换、连续展示、候选选择、拒绝 / 撤销、自动关系只读及既有导入状态。
- [x] 6.3 2026-09-04，比较基线 `4adf6824b2acbcf6ffc641e7da7f8068605d143a`，执行 `npm run build` 和 `git diff --check`：TypeScript、Vite 生产构建和 diff 检查均通过。
- [x] 6.4 2026-09-04，真实 Chromium 生产预览 QA，URL `http://127.0.0.1:5174/cash-import`：执行 `npm run test:e2e`，37/37 通过；新增用例覆盖配对主流程、`全部 / 自动 / 待处理` 摘要卡片点击（含键盘 Enter）、21 条连续关系、候选与类型选择、拒绝 / 撤销、桌面表格和移动卡片。视口 `320`、`375`、`390`、`414`、`768`、`1440 px` 均无页面横向滚动；截图：`/tmp/cash-import-relations-production-390.png`、`/tmp/cash-import-relations-production-1440.png`。配对用例控制台错误、页面异常和请求失败均为 0；既有正常 / 空 / 错误 / 加载 / 完成导入状态用例均通过。
- [x] 6.5 PostgreSQL 双后端契约矩阵不适用：本变更仅修改 Web 展示、筛选和响应式布局，无 Application Service、金额 / 币种语义、持久化、迁移或数据库查询变化。
- [x] 6.6 完成性能与安全范围检查：取消分页仅移除前端切片，不新增请求、外部资源、敏感日志、依赖或信任边界；21 条关系在浏览器中连续渲染通过。更大规模虚拟列表保留为后续独立议题。

## 7. 发布准备

- [x] 7.1 记录发布准备、观察项和回滚方案：恢复原分页状态与分页器、移除移动卡片媒体查询及摘要卡片样式即可回退；不需要数据迁移或回填。上线观察重点为超大关系量下的首屏渲染时间和移动端长列表滚动。
- [ ] 7.2 在获得用户明确授权后，按小步创建提交、推送、创建目标 PR 并合入指定分支；当前仅创建本地 change，未执行外部写入。

## 8. 反思

- [x] 8.1 完成实现后的复盘：统一关系数据与决定状态机后，用桌面表格 / 移动卡片两套呈现复用同一语义；摘要筛选复用导入预览卡片 token；取消分页后以单列连续列表满足移动浏览，后续仅在真实大数据量出现性能信号时引入虚拟列表；新增 E2E 覆盖多视口和无障碍交互，补强了测试与视觉证据。
